# ITER022 preregistration — RC006 source-grounded EPRL component primitives

Date: 2026-09-14

## Authorization and correction boundary

ITER020 authorizes a source-faithful component reconstruction checkpoint. ITER021 is terminally BLOCKED only because its preregistration incorrectly required unproven numerical sentinels. ITER022 does not inherit those numbers. Its predicates are taken directly from the byte-pinned arXiv:1609.02429v2 source environments extracted before this preregistration.

Primary source e-print SHA256 remains:
`3e04a41195e0313fd97c68fcb8cc2ec30fa4bdfb5529ba867923c4120fc3e0ee`.

## Frozen target conventions

Use the validated ITER019/020 q-CG solver for target `_q C`, without importing an external closed form or fitting signs/phases.

Target source facts used prospectively:

1. Appendix A orthogonality: `sum m1,m2 _q C(j1,j2,j;m1,m2,m) _q C(j1,j2,j';m1,m2,m') = delta_jj' delta_mm'` on admissible channels.
2. Appendix B cap/cup define `_qbar mathcal C` as the dual map of `_q mathcal C`; concatenating q/qbar maps gives `(-1)^(j1+j2-j3) d_j3^(-1) delta`.
3. Appendix B 4-valent basis is `sum_m5 _qbar mathcal C(j1,j2,j5) * _q mathcal C(j3,j4,j5)`; its source-defined dual carries `(-1)^(2j5) q^(m5)` and the closed basis-dual contraction is `(-1)^(j1+j2+j3+j4) d_j5^(-1) delta_(j5 j5')`.
4. Appendix E final EPRL normalization graph reduces to
   `(-1)^(sum_i(j_i^+ + j_i^-) - 2l) * (d_l1 d_l2 d_l3 d_l4)^(-1) * d_l^(-2) * delta_(l l')`.
5. At k=12, gamma=1/3, source nontrivial simplicity maps are `l=3 -> (j^+,j^-)=(2,1)` and `l=6 -> (4,2)`, in addition to the trivial spin assignment.

The Appendix-B graphical coefficient `mathcal C` differs from the Appendix-A coefficient `C` by `mathcal C = C / sqrt(d_output)`. This factor must be explicit in every component calculation.

## Frozen panels at k=12

Use twice-spin integers internally but report physical spins.

### Pair/dual panel
All admissible triples generated by:
- `(j1,j2)=(2,1)` with every admissible output;
- `(4,2)` with every admissible output;
- `(3,3)` with outputs `0,1,...,6`;
- `(3,6)` with its admissible output(s);
- `(6,6)` with its admissible output(s).

### 4-valent panel
- external `(3,3,3,3)`, every common admissible internal `l=0,1,...,6`;
- external `(3,6,3,6)`, every common admissible internal channel;
- external `(6,6,6,6)`, every common admissible internal channel;
- external `(0,3,0,3)`, every common admissible internal channel.

No panel may be changed after production output.

## Parallel lanes

### Lane A — qbar dual composition

Construct source-defined `_qbar mathcal C` mechanically by cap/cup bending of solver `_q mathcal C`, not by assuming ordinary complex conjugation. For every frozen pair/dual triple, contract the source q and qbar maps in the Appendix-B orientation.

PASS if the component matrix equals
`(-1)^(j1+j2-j3) / d_j3 * I`
to max absolute residual `< 2e-9`.

Also compare the bent source dual with a separately solved qbar-coproduct embedding only as a diagnostic up to the residual normalized channel sign; this diagnostic is not allowed to redefine the source dual.

### Lane B — 4-valent basis/dual identity

Build the Appendix-B 4-valent basis and its exact source-defined dual from Lane-A primitives for every frozen 4-valent panel. Contract all external magnetic indices.

PASS if the resulting internal-channel Gram matrix is diagonal and equals
`(-1)^(j1+j2+j3+j4) / d_l * delta_(l l')`
with max absolute residual `< 5e-9`.

### Lane C — Appendix-E E3 factorization

For source EPRL k=12, gamma=1/3 simplicity labels, combine the same component dual maps and 4-valent contractions into the exact closed normalization factor occurring in Appendix E for the frozen nontrivial sectors:
- all four external simplicity labels `l_i=3` with every admissible internal `l`;
- mixed source-admissible sector `(l1,l2,l3,l4)=(3,6,3,6)` with every common admissible `l`;
- `(6,6,6,6)` with every common admissible `l`.

Compute the closed factor in two independently coded ways:
1. explicit component contractions from q/qbar/4-valent primitives;
2. the extracted Appendix-E analytic reduction.

PASS if every diagonal case agrees to absolute/relative `1e-8`, all off-diagonal internal `l != l'` cases vanish to `<1e-9`, and independently flipping any allowed residual channel sign leaves the final closed scalar unchanged to `<1e-9`.

If a listed sector is source-inadmissible after exact coupling checks, report it as excluded by a frozen source predicate rather than forcing a tensor.

### Lane D — adversarial false-positive calibration

On the smallest nontrivial frozen sector, apply separately:
1. replace cap/cup-bent qbar dual by ordinary elementwise complex conjugation;
2. omit the `1/sqrt(d_output)` conversion between Appendix-A `C` and Appendix-B `mathcal C`;
3. remove the source `q^(m5)` factor from the 4-valent dual;
4. replace one quantum dimension by classical `2j+1`.

The correct construction must satisfy the relevant Lane-A/B/C predicate. PASS calibration requires at least three of four wrong constructions to produce a residual `>1e-6` against their source identity. No wrong variant receives physics interpretation.

## Aggregate

Run lanes A-D independently with `fail-fast:false`. Green CI alone is not a PASS. Every lane emits machine-readable evidence.

Terminal classes:
- `RC006_EPRL_COMPONENT_PRIMITIVES_PASS`
- `RC006_QBAR_COMPONENT_DUAL_FAIL`
- `RC006_4VALENT_COMPONENT_DUAL_FAIL`
- `RC006_E3_COMPONENT_FACTORIZATION_FAIL`
- `RC006_COMPONENT_CALIBRATION_FAIL`
- `RC006_COMPONENT_PRIMITIVES_INFRASTRUCTURE_PARTIAL`

Only full PASS authorizes preregistration of direct Eq.(27) triangular block reconstruction. It still does not authorize one-step TNR, Eq.(29)/Lambda, alpha selection, bridge credit, candidate theory or new physics.

## Locks

Always false during ITER022: `iter012_retry_authorized`, `one_step_tnr_authorized`, `eq29_amplitude_authorized`, `bridge_credit`, `candidate_theory_authorized`, `preferred_alpha_found`, `new_physics_found`.