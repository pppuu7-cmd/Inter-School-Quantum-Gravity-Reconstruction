# ITER083A preregistration — CDT graph ↔ continuum spatial-Laplacian normalization authority

Date: 2026-09-15
Gate: `ITER083A_CDT_GRAPH_TO_CONTINUUM_SPATIAL_LAPLACIAN_NORMALIZATION_AUTHORITY`

ITER082 established a genuine foliated-FRG spatial regulator operator but left the CDT graph ↔ continuum ADM spatial-Laplacian normalization/domain map open.

Frozen sources: arXiv `1804.02294`, `1903.00430`, `1912.11311`, `2306.10408`.

Required predicates: explicit discrete CDT Laplacian normalization; explicit continuum spatial Laplacian normalization; source-defined role of lattice spacing `a`; controlled continuum/finite-volume scaling; explicit conversion rule for eigenvalues; no target-fitted proportionality; no post-hoc mode selection.

Mandatory controls: graph combinatorial eigenvalue is not a physical continuum eigenvalue without scale conversion; `lambda_graph/a^2` is not authorized unless source-derived; thermodynamic scaling is not continuum normalization; phase-critical scaling is not FRG `k`; ADM spatial regulator choice does not itself supply lattice matching.

Frozen classifications: `PASS_SCOPED_SOURCE_DEFINED_GRAPH_CONTINUUM_NORMALIZATION`, `PASS_SCOPED_PARTIAL_NORMALIZATION_CONTINUUM_MATCH_OPEN`, `SCOPED_BLOCKED_NO_GRAPH_CONTINUUM_NORMALIZATION_AUTHORITY`, or `INFRASTRUCTURE_FAIL_SOURCE_EXTRACTION`.

No bridge credit, shared fixed point, full theory equivalence, new physics or candidate theory can follow from this gate.