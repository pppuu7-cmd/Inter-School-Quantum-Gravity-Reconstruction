# ITER115 preregistration — fixed-geodesic curvature `O(G^2)` RG/log hierarchy authority

Date: 2026-09-15
Gate: `ITER115_FIXED_GEODESIC_CURVATURE_G2_RG_LOG_HIERARCHY_AUTHORITY`

## Motivation frozen before residue inference

ITER114 restricts the separated anchored correlator to

`C(l;mu)=G^2 l^-8 [A_0(mu)+A_1(mu)L+A_2(mu)L^2]+O(G^3)`,

`L=log(mu^2 l^2)`.

The explicit fixed-geodesic matter calculation shows that the double logarithm is generated by a double UV pole and that `mu`-independence links its coefficient to the running finite geodesic counterterm. ITER115 asks what can be concluded for curvature from RG consistency alone, without importing matter coefficients.

## Frozen authority

- arXiv:1706.01891, especially equations defining `H^(1),H^(2)`, their `mu` derivatives, and the running finite geodesic counterterm.
- ITER114 radial basis.
- 2026 master-coordinate curvature result only for the fact that the separated `RR` tree/lower-order terms are contact and the first master-coordinate noncontact term is `O(G^2)`.

## Required predicates

A. Derive the RG consistency relations among `A_0,A_1,A_2` for nonzero separation.

B. Distinguish explicit `mu` dependence through `L` from running of finite observable-specific coefficients.

C. Determine whether `A_2` is RG invariant at the frozen order.

D. Establish whether a nonzero `A_2` would be sufficient to prove a nonzero `O(G^2)` separated tail independent of `A_0,A_1`.

E. Test whether the matter-scalar source authorizes importing a nonzero `A_2` into curvature. The fact that the curvature tree correlator is contact-only must be retained.

F. If `A_2` cannot be inferred, identify the cheapest source-independent calculation needed to determine it (e.g. highest-pole residue rather than complete finite integrals).

G. No EDT target may be used to choose the sign/value of any coefficient.

## Frozen classifications

- `PASS_SCOPED_RG_HIERARCHY_A2_NONZERO_PROTECTED_NONCANCELLATION` if A-F establish and source-fix `A_2 != 0`.
- `PASS_SCOPED_RG_HIERARCHY_HIGHEST_LOG_DIAGNOSTIC_A2_VALUE_OPEN` if the hierarchy and sufficiency of nonzero `A_2` are established but curvature-specific `A_2` remains uncomputed.
- `PASS_SCOPED_RG_HIERARCHY_ONLY_NO_HIGHEST_POLE_SHORTCUT` if even `A_2` requires the full finite calculation.
- `BLOCKED_SOURCE_AUTHORITY` if the radial coefficients cannot be given consistent RG meaning.

## Controls

- `MATTER_A2_CURVATURE_A2_SWAP_CONTROL`
- `TREE_NONCONTACT_ERASURE_CONTROL`
- `MU_EXPLICIT_RUNNING_SWAP_CONTROL`
- `EDT_LOG_COEFFICIENT_FIT_CONTROL`

## Claim ceiling

A PASS below the first classification does not prove noncancellation. No direct EDT/EFT conflict, continuum EDT, `BRIDGE_DERIVED`, new physics or candidate theory follows.