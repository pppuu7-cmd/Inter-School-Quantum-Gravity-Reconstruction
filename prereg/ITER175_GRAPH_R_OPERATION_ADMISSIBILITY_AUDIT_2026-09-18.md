# ITER175 preregistration — graph-level R-operation admissibility/source-object audit

Frozen before execution. Parent: terminal ITER174 BLOCKED.

Goal: determine whether the existing pre-ITER170 source contains enough explicit information to **define a source-faithful graph-level extension/R-operation object for the unseparated first-M/G amplitude**. This is a read-only prerequisite audit, not an R-operation evaluation.

Required fields, all with executable/source-qualified provenance: (1) graph/subgraph identity plus nesting/overlap relation; (2) unseparated loop and affine integration variables with measure/Jacobian; (3) propagator denominator powers and numerator structure before contact splitting; (4) endpoint embedding/phase map; (5) frozen subtraction order or forest relation; (6) mathematically explicit extension/renormalization prescription applicable to the disputed endpoint/contact object.

Three independent lanes: `graph`, `measure_embedding`, `forest_prescription`; `fail-fast:false`. Post-ITER169 recovery/prereg/result prose cannot fill a field. Lexical hits are evidence candidates only and require semantic adjudication.

Scientific scoped PASS requires all six fields and no contradiction. If any field is absent or only qualitative, classification is `BLOCKED_SCOPED_ITER175_GRAPH_R_OPERATION_PREREQUISITES_INCOMPLETE` with an exact ledger. No threshold may be changed after execution.

Claim ceiling: even PASS authorizes only a later prospectively frozen construction gate. ITER175 itself does not compute Laurent coefficients, pole tensors, ITER118 matching, `B1_total`, bridge credit, or candidate theory.