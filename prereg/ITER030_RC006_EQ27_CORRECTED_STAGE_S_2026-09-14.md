# ITER030 preregistration — RC006 corrected Eq.(27) Stage-S source graph/component authority

Date: 2026-09-14

Status: `PREREGISTERED_NOT_EXECUTED`.

## Authorization basis

ITER029 terminalized `PASS — RC006_EQ27_SOURCE_SEMANTICS_RECONCILED_SCOPED`. It established prospectively that the exact byte-pinned Eq.(27) display contains **two** bracketed TikZ graphical factors/environments and two internal sum structures; the historical count four refers to two begin plus two end syntax tokens. ITER028 remains terminal BLOCKED and is not modified.

ITER030 is a new scientific object. It asks whether the exact source determines a complete graph-to-component/index map for those two factors. It does not execute the amplitude contraction.

## Frozen source authority

Primary source: arXiv:1609.02429v2.

Required archive SHA256:
`3e04a41195e0313fd97c68fcb8cc2ec30fa4bdfb5529ba867923c4120fc3e0ee`.

Required exact Eq.(27) display SHA256:
`88d2f7be9489980a763dd66b5c4900473c79900ccf72790095ae1675e9733b2d`.

A source-hash mismatch is `INFRASTRUCTURE_OR_PROVENANCE_BLOCKED`, never scientific FAIL.

## Frozen hypothesis H030

The byte-pinned Eq.(27) object plus its explicitly cited Appendix-B/E/F algebraic identities mechanically determines an executable component dictionary for both graphical factors, including every required tensor leg/index order, internal representation/magnetic summation, loop attachment, crossing orientation, primed/unprimed channel distinction, scalar prefactor, normalization factor and source locator, without visual guesswork, fitted phases, post-hoc tensor permutations or numerical agreement.

## Four independent lanes

All lanes use the same immutable byte-pinned source but answer independent questions and may run in parallel with `fail-fast:false`.

### Lane A — corrected source inventory
PASS requires exactly one Eq.(27) display; exactly 2 TikZ environments; exactly 2 draw commands; exactly 2 bracketed graphical factors; exactly 2 source sum structures; source hashes equal the frozen values. The historical four begin/end tokens are recorded but are not graph factors.

### Lane B — mechanical topology/endpoints
For each of the 2 graphical factors, mechanically inventory all TikZ nodes/draw paths, named endpoints, loops, crossings, primed/unprimed labels and representation labels. Every endpoint and connection required for a component network must have a stable source locator. Geometry alone cannot supply a tensor index.

PASS requires no unresolved named endpoint, no unaccounted open leg, and a mechanically source-located attachment for each loop/crossing/channel connection.

### Lane C — component/index authority
Audit the exact Eq.(27) context and explicitly cited Appendix-B/E/F identities for each required ordered tensor leg and magnetic/internal index relation. A graph line with no textual/algebraic index-order authority is unresolved even if visually clear.

PASS requires every contraction edge to have an ordered component-level index relation and every summed index/range to be source-qualified. Any missing relation yields `BLOCKED_INDEX_AUTHORITY`.

### Lane D — scalar/source-anchor and null controls
Mechanically inventory every scalar prefactor, sign, quantum-dimension/normalization marker adjacent to Eq.(27), and the source anchors needed to interpret them. Also test preregistered false maps: (i) treating four begin/end tokens as four graph factors; (ii) collapsing two factors into one; (iii) accepting a draw path without component/index authority; (iv) erasing primed/unprimed channel distinction. All four wrong interpretations must be rejected.

## Aggregate classes

### PASS — `RC006_EQ27_CORRECTED_STAGE_S_COMPONENT_MAP_PASS`
Only if A–D all PASS and the aggregate dictionary contains exactly two graphical factors with complete source-located component/index authority and no unresolved item. This PASS authorizes a separately preregistered bounded numerical Eq.(27) component contraction using the immutable dictionary. It gives zero bridge/TNR/candidate-theory credit.

### BLOCKED — `RC006_EQ27_CORRECTED_STAGE_S_COMPONENT_MAP_BLOCKED`
If source hashes and the corrected two-factor inventory are valid but any exact endpoint, loop attachment, crossing direction, tensor/index order, internal summation range, normalization/source anchor, or component relation remains unavailable/ambiguous.

### FAIL — `RC006_EQ27_CORRECTED_STAGE_S_SOURCE_CONTRADICTION`
Only if the byte-pinned source and durable authority unambiguously contradict the frozen corrected two-factor hypothesis or a source-qualified identity fails mechanically. Missing authority is BLOCKED, not FAIL.

### Infrastructure/provenance
Transport/parser failure before substantive source evaluation is `NUMERICAL_OR_INFRASTRUCTURE_FAIL_PRE_SCIENCE`; source-hash mismatch is `INFRASTRUCTURE_OR_PROVENANCE_BLOCKED`. Neither counts as a scientific result.

## Frozen anti-retuning rules

No threshold/model/convention may be changed after substantive output. No tensor permutation, R/R^-1 choice, qbar orientation, phase/sign, alpha or normalization may be selected by agreement with a desired scalar. Eq.(29)/Lambda and historical one-step TNR are forbidden.

## Claim locks

Always false during ITER030: `iter012_retry_authorized`, `eq29_amplitude_authorized`, `one_step_tnr_authorized`, `bridge_credit`, `candidate_theory_authorized`, `preferred_alpha_found`, `new_physics_found`, `ALL_KNOWN_SCHOOLS_FAIL`, `NEW_QG_THEORY_REQUIRED`, `UNIVERSAL_BRIDGE_FOUND`.

Candidate theory remains `UNFORMED / 0%`; readiness cannot increase from Stage-S authority alone.
