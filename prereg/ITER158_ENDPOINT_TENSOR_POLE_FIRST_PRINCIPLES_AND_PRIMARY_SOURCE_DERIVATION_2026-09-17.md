# ITER158 preregistration — endpoint tensor pole first-principles and primary-source derivation

Date: 2026-09-17
Gate: `ITER158_ENDPOINT_TENSOR_POLE_FIRST_PRINCIPLES_AND_PRIMARY_SOURCE_DERIVATION`
Frozen parent `main`: `bb57773d0711b9facab6b3316bb2e6ecd8b645f2`

## Frozen scientific objective

ITER157 terminalized `BLOCKED_SCOPED_ITER157_NO_SOURCE_FAITHFUL_ENDPOINT_TENSOR_POLE_EQUATION_FOUND`. ITER158 must therefore **create or acquire new mathematical information**, not repeat repository grep/audit.

For each frozen first-M/G family, determine whether a source-faithful general-d divergent endpoint tensor residue can be derived from frozen microscopic objects or imported from primary literature with an exact convention map. The target object is

`T_endpoint,pole = Sum_i c_i B_i`

or a full tensor relation whose prospectively frozen projections determine the coefficients, with `B_i` in the immutable ITER118 endpoint basis.

## Frozen endpoint basis and order

`[R,S,DR,DS,BoxR,D2R,BoxS,D2S]`, where `S=R_mn n^m n^n`, `DR=n.nabla R`, `DS=n.nabla S`, `D2R=(n.nabla)^2 R`, `D2S=(n.nabla)^2 S`.

No basis direction may be added, deleted, or permuted after any pole/equation outcome is seen.

## Frozen graph families

Use the canonical repository definitions, not shorthand prompt memory:

- `M_R2_chi1_dR1`
- `M_R1_chi1_dR2`
- `G_R1_chi2_Gamma2_dR1`

For each family persist exact insertion identity, propagators, vertices, external tensor structure, endpoint orientation, loop measure/normalization, powers of `i`, symmetry factor and inherited ITER154B proper-subdivergence status.

## Parallel acquisition routes frozen before outcome

### Route A — primary literature

Search original calculations specifically for fixed-geodesic/worldline endpoint UV divergences, gravitational Wilson/geodesic endpoint counterterms, tensor-resolved endpoint renormalization, one-loop endpoint operator mixing, or boundary/heat-kernel coefficients only when exact equivalence to the repository object is established.

A source is coefficient authority only if an exact source-to-repository map is proven for field content, observable, graph topology, regulator, endpoint condition, normalization, metric/Riemann/Ricci/Fourier conventions, tangent normalization/orientation, dimensional convention and tensor basis. Otherwise classify it `REFERENCE_ONLY`.

### Route B — first-principles repository derivation

Recover the earliest source-faithful microscopic object upstream of scalar invariants:

`frozen fields/propagators/vertices -> uncontracted tensor numerator -> regulated endpoint kernel -> raw local pole tensor -> support classification -> endpoint tensor equation`.

If an irreversible scalarization or missing microscopic primitive prevents reconstruction, identify the earliest exact lossy/undefined map and the minimum missing primitive. Do not invent an inverse.

## Source/convention manifest freeze rule

Before accepting any coefficient/equation from a source, persist source ID/version/DOI/arXiv as applicable, page/equation/section, metric signature, Riemann/Ricci conventions, Fourier convention, loop normalization, dimensional convention and epsilon definition, endpoint orientation, tangent normalization, geodesic/worldline parameter, propagator/vertex normalization, symmetry factor and exact source-to-repository conversion map.

No silent conversion is admissible.

## General-d and pole-order firewall

Use repository convention `d=4-2 epsilon` unless a frozen microscopic source proves a different convention for the same object and an exact translation is recorded. Extract UV poles before setting `d=4`. Keep distinct `1/epsilon^2`, `1/epsilon`, finite and `O(epsilon)` terms. Finite counterterm freedom cannot alter a frozen pole coefficient.

## ITER154B lock

For the exact three connected-cross primitive bubbles, strict proper ordinary bulk/local-composite subdivergence counterterm insertions vanish in the scoped ITER154B sense, so `R_sub G = G` only for that proper-subdivergence operation. This does not imply the whole bubble, endpoint, line or contact sector is finite or zero.

## Raw tensor residue first

Do not fit ITER118 coefficients directly. Persist each raw regulated tensor integrand and then raw divergent tensor residue before endpoint-basis projection. Record free indices, metric contractions, tangent vectors, normal projectors, momenta/derivatives, curvature tensors, full d dependence, pole order and normalization.

## Support firewall

Classify every divergent term before solving as exactly one of:

- `BULK`
- `LINE_DEFECT`
- `ENDPOINT`
- `CONTACT_DISTRIBUTION`
- `UNRESOLVED`

Slot 7 consumes only `ENDPOINT` terms.

## ITER153 contact firewall

The seven ITER153 ambient contact distributions remain separate unresolved renormalized objects. Do not convert them into endpoint coefficients without a separately authorized renormalized mapping. Do not set endpoint separation to zero before pole extraction/support classification and do not use a premature `scaleless = 0` shortcut.

## Projection manifest firewall

Before any coefficient rank is examined, freeze all projection functionals or a deterministic exact projection-generation algorithm. Every accepted equation must be persisted as

`P_alpha[T_raw] = Sum_i A_alpha_i c_i = r_alpha`

with projector definition, source family/stratum, pole order, exact row `A_alpha_i`, exact residue `r_alpha`, raw-tensor content hash and equation content hash. No projection may be added after observing a null direction.

If generic exact curvature jets are used, freeze deterministic rational seeds before coefficient solving, require curvature identities, preserve all eight basis directions, use an overdetermined construction set and held-out exact jets, and forbid relying only on symmetric backgrounds.

## Independent Critic

At least one representative stratum must be reconstructed independently from source-level objects. Preferred split: producer momentum-space regulated tensor derivation; Critic independent covariant/local expansion. If a genuinely independent method is unavailable, document the dependence and do not claim independent reconstruction.

## Frozen negative controls

Reject derivations with any of: `d=4` before pole extraction; missing symmetry factor; wrong endpoint orientation; wrong tangent normalization; endpoint/line or endpoint/contact swap; basis permutation/drop; wrong Riemann sign; finite term used as pole; null direction interpreted as zero; post-outcome projection addition; source equation imported without complete convention map.

## Frozen terminal classes

### PASS

`PASS_SCOPED_ITER158_ENDPOINT_TENSOR_POLE_EQUATIONS_DERIVED`

iff at least one genuinely new source-faithful tensor-resolved endpoint pole equation is obtained and persisted with raw tensor provenance, support classification, exact normalization/convention map and independent Critic validation. PASS does not itself close slot 7 and does not authorize rank fitting unless the complete intended equation manifest for the follow-up solve is frozen first.

### BLOCKED

`BLOCKED_SCOPED_ITER158_EXACT_MISSING_PRIMITIVE_LOCALIZED`

iff no admissible endpoint equation can be completed without invention. The result must name the **minimum exact earliest missing primitive**, such as `UNCONTRACTED_TENSOR_NUMERATOR_MISSING`, `ENDPOINT_VERTEX_NORMALIZATION_MISSING`, `REGULATOR_PRESCRIPTION_INCOMPLETE`, `ENDPOINT_RESTRICTION_UNDEFINED`, `GENERAL_D_ENDPOINT_OPERATOR_UNDEFINED`, or `PRIMARY_SOURCE_CONVENTION_NOT_TRANSLATABLE`, and must identify the exact first lossy/undefined map.

A generic `more information required`/`source missing` classification is forbidden.

### INVALID

`INVALID_ITER158` for preregistration contamination, basis mutation, post-outcome projection choice, provenance/convention failure, or implementation error.

Use `SCIENTIFIC_FAIL` only if a completed source-faithful derivation contradicts a prospectively frozen structural identity; inability to source a primitive is BLOCKED.

## Successor firewall

Only after ITER158 PASS and immutable freeze of the complete equation/projection manifest may a separate preregistered rank/coefficient gate run. No rank/nullspace computation may influence equation acquisition.

## Claim locks

`B1_total = UNAUTHORIZED`; `BRIDGE_DERIVED=false`; `NEW_PHYSICS_FOUND=false`; `candidate_theory_authorized=false`; candidate theory remains `UNFORMED / 0%`.
