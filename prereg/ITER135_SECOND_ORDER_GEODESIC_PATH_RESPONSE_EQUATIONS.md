# ITER135 — second-order geodesic/path-response equation authority gate

Status: **PREREGISTERED BEFORE PRODUCTION COMPUTE**
Date: 2026-09-15

## Question
In the same frozen 4D Euclidean linear metric split `g_ab = delta_ab + kappa h_ab`, can the fixed-endpoint geodesic equation be expanded through O(kappa^2) into an explicit target-blind boundary-value system whose first-order solution and second-order source uniquely determine the path-response contribution needed by physical chi2?

## Frozen geometry and convention
Use background segment `z0^a(t)=x^a+t L n^a`, t in [0,1], with fixed coordinate endpoints. Expand `z=z0+kappa xi1+kappa^2 xi2+...` and impose Dirichlet endpoint conditions `xi_r(0)=xi_r(1)=0`. Use the affine geodesic equation with Christoffel symbols generated by the frozen linear metric split. No post-result change of endpoint convention, parameterization, gauge proxy, or metric split is allowed in this gate.

## Frozen tasks
1. Derive the O(kappa) equation `xi1'' = S1[h;z0]` explicitly from Gamma1.
2. Construct the Dirichlet Green function for d^2/dt^2 on [0,1] and verify the endpoint conditions and derivative jump exactly.
3. Express xi1 as an explicit nonlocal line integral of S1.
4. Derive the complete O(kappa^2) source census for xi2, including Gamma2(z0) z0' z0', the displacement derivative `(xi1·partial)Gamma1(z0) z0' z0'`, and velocity-response terms `2 Gamma1(z0) z0' xi1'`, with signs fixed by the same geodesic equation.
5. Verify every O(kappa^2) source is bilinear in h after substituting xi1[h].
6. Check endpoint reversal covariance t->1-t, n->-n for S1/xi1 and the complete second-order source.
7. Perform at least three held-out plane-wave/polarization tests comparing symbolic source construction against direct perturbative expansion of the geodesic equation to relative tolerance <=1e-10 or exact algebraic equality.
8. No B1, EDT exponent, desired sign, bridge target, or fitted coefficient may appear in derivation/tests.

## Classification
All tasks 1–8 with a unique fixed-endpoint affine response: `PASS_SCOPED_SECOND_ORDER_PATH_RESPONSE_EQUATIONS_CLOSED_CHI2_ASSEMBLY_OPEN`.

A genuine algebraic disagreement between the prospectively defined perturbative equations and direct expansion: `SCIENTIFIC_FAIL_SECOND_ORDER_PATH_RESPONSE_EQUATIONS` unless a purely technical implementation error is demonstrated without changing frozen science.

If the fixed-endpoint affine convention itself fails to define the physical localization object required by ITER134, but the equations are internally consistent: `PASS_SCOPED_EQUATIONS_CONVENTION_TO_PHYSICAL_CHI2_AUTHORITY_OPEN`; no chi2 credit.

Infrastructure/runtime defects are `NUMERICAL/INFRASTRUCTURE FAIL` and may be repaired minimally without changing this prereg.

## Claim locks
This gate cannot establish B1, EDT agreement, bridge derivation, new physics, or candidate theory. Candidate theory remains 0/UNFORMED and bridge credit remains 0.
