# ITER034 preregistration — RC006 Eq.(27) source label/domain authority

Date: 2026-09-14
Status: `PREREGISTERED_NOT_EXECUTED`.

ITER033 terminalized `PASS — RC006_EQ27_SOURCE_SUMMATION_SCALAR_STRUCTURE_PASS` and authorizes exactly this source-authority prerequisite before any bounded numerical scalar/summation evaluation.

## Scientific question

Does the byte-pinned Eq.(27) source plus already validated source dictionaries explicitly provide a non-retuned numerical meaning/domain for every label needed by the two source sums and external prefactor: `j`, `j_i^+`, `j_i^-`, `l`, `l_1`, `l_2`, `d_x`, q and alpha? No missing relation may be supplied from Eq.(29)/Lambda, fitted from output, or guessed from notation.

## Frozen lanes (`fail-fast:false`)

A. `external-labels`: inventory `l,l1,l2,alpha,d_l,d_l1,d_l2` and require source-qualified definitions/domain references already present in the exact source or validated repo source notes.

B. `factor1-labels`: inventory `j,j2+,j1-` plus `d_j,q`; require an explicit source-qualified mapping/domain sufficient to evaluate factor-1 weight, not merely lexical occurrence.

C. `factor2-labels`: inventory `j,j1+,j2-` plus `d_j,q`; same criterion independently.

D. `domain-null`: verify the authority detector rejects three predetermined insufficient substitutes: lexical occurrence without definition, Eq.(29)/Lambda-only mapping, and a fabricated equality between plus/minus labels.

Each positive lane is PASS only when every required label has a source-qualified executable/domain meaning without post-hoc inference. Otherwise it is `BLOCKED_SOURCE_LABEL_DOMAIN`, not scientific FAIL. Null lane PASS requires 3/3 insufficient substitutes rejected.

Aggregate PASS `RC006_EQ27_SOURCE_LABEL_DOMAIN_AUTHORITY_PASS` authorizes only a separately preregistered bounded numerical scalar/summation lift. BLOCKED preserves all prior positive results but forbids numerical completion by convention fitting.

Claim locks remain: `full_eq27_amplitude_derived=false`, `eq29_amplitude_authorized=false`, `one_step_tnr_authorized=false`, `bridge_credit=false`, `candidate_theory_authorized=false`, `new_physics_found=false`, `ALL_KNOWN_SCHOOLS_FAIL=false`, `NEW_QG_THEORY_REQUIRED=false`, `UNIVERSAL_BRIDGE_FOUND=false`.

Candidate theory remains `UNFORMED / 0%`; overall roadmap readiness remains 49%.
