# Preregistration — ITER043 RC006 duality exact-formula qualification

Date frozen: 2026-09-14

## Inherited status

ITER039 remains `SCIENTIFIC_FAIL` (`RC006_EQ27_BOUNDED_CENTRAL_COMPONENT_CLOSURE_FAIL`). ITER041 remains scoped source-BLOCKED on the exact expanded-domain dual/cap/cup normalization identity. ITER042 terminated `DELEGATED_AUTHORITY_SOURCE_EXPANSION_CANDIDATES_FOUND`: it found inspectable full-text candidates but explicitly did not qualify a formula or authorize numerics.

This iteration is **source-formula qualification only**. No numerical correction, threshold change, tuple removal, fitted phase, fitted normalization, or convention selection by agreement with ITER039 is permitted.

## Frozen source panel

The source panel is fixed prospectively to the ITER042 discoveries and delegated authorities:

A. R. Dowdall, *Spin foam models for 3D quantum geometry*, OpenAlex `W2131194795`, Nottingham open thesis PDF `http://eprints.nottingham.ac.uk/11755/1/RDowdall-thesis-14-1-11.pdf`.

B. I. Coman, M. Gabella, J. Teschner, *Line operators in theories of class S, quantized moduli space of flat connections, and Toda field theory*, arXiv `1505.05898` / OpenAlex `W3021373932` (duplicate published record `W2100639234`).

C. Delegated book records only: Biedenharn–Lohe, DOI `10.1142/2815`, and Carter–Flath–Saito, DOI `10.1515/9780691234670` / `10.2307/j.ctv1nxcv83`. These may qualify only if an inspectable formula-bearing preview/full text is actually obtained; metadata or table-of-contents records do not qualify.

No additional source may silently replace this panel. A newly encountered citation may be recorded as a future candidate but cannot make ITER043 PASS.

## Frozen scientific question

Does any frozen source provide, in an inspectable and citable passage, enough explicit mathematics to source-qualify the expanded-domain q-CG duality/cap/cup contraction needed after ITER040/041, **including the domain in which the identity is valid**?

The needed object is not merely a statement that duality or orthogonality exists. Qualification requires an explicit equality/identity with identifiable representation/magnetic labels or an unambiguous graphical equality whose normalization and domain are stated in the same source context.

## Independent lanes

`fail-fast:false` with four independent lanes:

1. `dowdall-formula`: obtain the frozen thesis, extract text, preserve pages/line neighborhoods around q-CG/Clebsch-Gordan, root-of-unity/level-k, dual/cap/cup, orthogonality/completeness, negligible/trace-zero/projector language.
2. `coman-formula`: obtain arXiv `1505.05898`, extract text/source, preserve neighborhoods around the same frozen concept families.
3. `delegated-preview`: probe the frozen delegated DOI/chapter endpoints for legally accessible preview/full-text formula material; preserve accessibility and exact provenance. Metadata-only is a non-qualifying result, not infrastructure failure.
4. `null-provenance`: verify that qualification logic rejects (i) source with only keyword co-occurrence and no equality, (ii) equality without root-of-unity/domain scope, (iii) catalog metadata, and (iv) a deliberately absent/fake source identifier.

## Frozen qualification predicates

A source can become `QUALIFIED_FORMULA_CANDIDATE` only if all of the following are evidenced in preserved source text/context:

Q1. q-deformed Clebsch–Gordan/intertwiner or equivalent trivalent coupling object is explicit.

Q2. an explicit duality/cap/cup, orthogonality/completeness, or bending contraction equality is present; lexical mention without an equality/graphical identity is insufficient.

Q3. normalization factors/sign/q-power/quantum-dimension factors appearing in the identity are visible enough to implement without choosing them from numerical agreement.

Q4. the validity domain is explicit enough to distinguish generic-q from the semisimple/root-of-unity/level-k quotient relevant to the ITER040 expanded-domain question. A generic-q identity alone cannot qualify the root-of-unity expanded-domain use.

Q5. source provenance is exact: title/identifier plus page/section/equation or source-line neighborhood.

The workflow may mechanically collect candidate excerpts and conservative predicate evidence. **Ambiguity is BLOCKED, not PASS.** No numerical output from ITER039 may be consulted by qualification logic.

## Terminal outcomes

- `RC006_DUALITY_EXACT_FORMULA_QUALIFIED_SCOPED` only if at least one frozen source satisfies Q1–Q5 without post-hoc convention selection.
- `RC006_DUALITY_EXACT_FORMULA_BLOCKED` if transport succeeds but no frozen source satisfies Q1–Q5 or source access is metadata/preview-insufficient.
- `INFRASTRUCTURE_FAIL` only when transport/tooling prevents the frozen lanes from producing their evidence packets.

Even a PASS authorizes only a separate prospectively preregistered implementation-validation gate. It does **not** reverse ITER039 and does not directly authorize a numerical retry.

## Claim locks

`ALL_KNOWN_SCHOOLS_FAIL=false`; `NEW_QG_THEORY_REQUIRED=false`; `NEW_PHYSICS_FOUND=false`; `BRIDGE_DERIVED=false`; full Eq.(27) amplitude not derived; Eq.(29)/Lambda/TNR unauthorized; candidate theory = `0 / UNFORMED`; bridge credit = 0.