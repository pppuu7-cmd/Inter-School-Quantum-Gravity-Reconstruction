# ITER116 source authority — corrected fixed-geodesic curvature `O(G^2)` single-log RG hierarchy

Date: 2026-09-15
Gate: `ITER116_FIXED_GEODESIC_CURVATURE_G2_SINGLE_LOG_RG_HIERARCHY_AUTHORITY`
Preregistration: `ec44eb92a104aaa4a2e41039bf031377404daece`

## 1. Correct separated basis

From corrected ITER114,

`C(l;mu)=G^2 l^-8 [B_0(mu)+B_1(mu)L]+O(G^3)`,

with

`L=log(mu^2 l^2)`.

All contact distributions are excluded from this nonzero-separation equation.

## 2. RG consistency at fixed perturbative order

At fixed physical separation, `mu dL/dmu = 2`. To the frozen `O(G^2)` accuracy, running effects that would multiply the entire `G^2` prefactor first affect higher perturbative order or can be absorbed into the observable-specific coefficient running used below.

Demanding `mu dC/dmu=0` at nonzero separation gives

`0 = beta_0 + beta_1 L + 2 B_1 + O(G)_{higher order}`,

where

`beta_i := mu dB_i/dmu`.

Since this must hold for arbitrary `L`,

**`beta_1 = 0`**

and

**`beta_0 = -2 B_1`**

at the frozen order.

Thus the single-log coefficient is RG invariant at this order, while the finite constant must run so as to cancel the explicit logarithm.

Predicates A-C: **PASS_SCOPED**.

## 3. Match to the fixed-geodesic source template

The 2018 fixed-geodesic source writes the renormalized one-loop result as a coefficient multiplying `H^(2)` plus a finite running geodesic-renormalization coefficient multiplying `H^(1)`, together with contact terms.

Its source identities include

`mu d H^(2)/dmu = 4 H^(1)`

and, at separated points,

`mu d H^(1)/dmu = 0`

(the full distributional derivative is contact).

The finite geodesic parameter runs precisely to cancel the explicit scale dependence of the `H^(2)` term. After evaluating the outer Laplacian at nonzero separation,

- `H^(2)` supplies the **single-log** radial term plus a constant;
- `H^(1)` supplies a constant radial term.

This is exactly the corrected `B_0+B_1 L` hierarchy.

## 4. Nonzero B1 would settle noncancellation immediately

If `B_1 != 0`, then no choice of the finite constant `B_0` can make

`B_0+B_1 L`

vanish for all separated `l`. At most it can cross zero at one scale. Therefore a nonzero logarithmic coefficient is sufficient to prove that the complete `O(G^2)` separated fixed-geodesic curvature correlator is not identically zero.

Predicate D: **PASS**.

This would close ITER109's cancellation loophole without requiring the full finite constant.

## 5. B1 is controlled by the highest-pole / H^(2)-type residue

In the fixed-geodesic dimensional-renormalization template, the `H^(2)` distribution is generated by the strongest double-pole structure. The simple-pole / finite `H^(1)` sector does not produce a separated logarithm after its outer Laplacian is evaluated.

Consequently, after curvature derivatives/tensor reduction, the coefficient `B_1` is proportional to the **net coefficient of the H^(2)-type / highest-pole nonlocal structure** in the complete F+M+G calculation.

Thus `B_1` can in principle be determined without evaluating the full finite `B_0` sector: one must compute the curvature-specific highest-pole residue across all mandatory nonlocal classes and preserve the geodesic renormalization bookkeeping.

Predicate E: **PASS_DIAGNOSTIC**.
`HIGHEST_POLE_FINITE_CONSTANT_SWAP_CONTROL`: **PASS_CONTROL**.

## 6. Matter-scalar nonzero residue cannot be imported

The 2018 matter calculation has a nonzero separated tree propagator and obtains a nonzero `H^(2)` coefficient. The scalar-curvature two-point function is different: its lower-order separated contributions are contact-only in the low-energy calculation, and curvature insertions bring additional derivative/composite structures.

Therefore the matter value and even its nonzero sign cannot be transferred to curvature.

The only authorized conclusion is structural: **which residue controls `B_1`**, not its curvature value.

Predicate F: **PASS_CONTROL / B1 VALUE OPEN**.
`MATTER_B1_CURVATURE_B1_SWAP_CONTROL`: **TRIGGERS**.
`TREE_NONCONTACT_ERASURE_CONTROL`: **PASS_CONTROL**.

## 7. Cheapest curvature-specific calculation

The minimal noncancellation test is now smaller than the full one-loop finite calculation:

1. generate only the UV/highest-pole parts of F/M/G through `O(kappa^4)`;
2. reduce them onto the `H^(2)`-type radial distribution after complete gauge-consistent summation;
3. include the geodesic embedding renormalization needed to define the net residue;
4. extract the single number/tensor-reduced coefficient proportional to `B_1`.

Finite `H^(1)` pieces and finite master integrals contributing only to `B_0` are unnecessary for the binary question `B_1 != 0?`.

If the net highest-pole residue vanishes, one must then compute `B_0` to decide whether the whole `O(G^2)` separated correlator vanishes.

Predicate G: **PASS / STRICTLY SMALLER DIAGNOSTIC**.

## 8. Target independence

No EDT exponent, fit window or amplitude is used to infer `B_1` or select counterterms.

Predicate H: **PASS_CONTROL**.
`EDT_LOG_COEFFICIENT_FIT_CONTROL`: **PASS_CONTROL**.

## Source classification

**`PASS_SCOPED_SINGLE_LOG_RG_HIERARCHY_HIGHEST_POLE_DIAGNOSTIC_B1_VALUE_OPEN`**

The RG structure is now reduced to two coefficients with one particularly cheap diagnostic:

- `B_1` is RG invariant at this order and controlled by the net highest-pole / `H^(2)` residue;
- `B_0` runs as `mu dB_0/dmu=-2B_1`;
- `B_1 != 0` would prove a nonzero `O(G^2)` separated tail;
- the curvature-specific value of `B_1` is not supplied by the frozen sources.

## Highest-information successor

Preregister a curvature-specific **highest-pole residue census/evaluation gate**. It should compute only the coefficients of the double-pole/H^(2)-type structures in F/M/G, verify gauge-parameter cancellation of the net residue, and stop before finite one-loop integration. This is the cheapest remaining route to closing the noncancellation loophole.

## Claim ceiling

Bridge credit remains **0**. Candidate theory remains **UNFORMED / 0%**. No value/sign of `B_1`, no noncancellation theorem, no EDT fit or direct EDT/EFT conflict, no continuum EDT, `BRIDGE_DERIVED`, or new physics follows.