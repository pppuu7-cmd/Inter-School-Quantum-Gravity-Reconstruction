# ITER124 source authority — projected RG/pole calculation specification

Date: 2026-09-15
Gate: `ITER124_FIXED_GEODESIC_CURVATURE_PROJECTED_RG_POLE_CALCULATION_SPEC`
Preregistration: `prereg/ITER124_FIXED_GEODESIC_CURVATURE_PROJECTED_RG_POLE_CALCULATION_SPEC_2026-09-15.md`
Machine manifest: `analysis/iter124_projected_rg_pole_manifest.json`
Validator: `analysis/iter124_validate_rg_pole_manifest.py`
Workflow: `.github/workflows/iter124_projected_rg_pole_manifest.yml`
Artifact: `iter124-projected-rg-pole-manifest`

## 1. Sector completeness

The frozen contract retains all source-required perturbative sectors identified by ITER112–123:

- `F`: ordinary curvature/field, graviton/ghost interactions and bulk EFT counterterms;
- `M`: first-order geodesic/localization corrections;
- `G`: second-order geodesic/localization corrections;
- endpoint/local curvature-composite renormalization;
- the full four-operator line basis during renormalization, with the ITER120 field-redefinition quotient applied only to physical matching data after consistency is checked.

The two genuine line representatives remain

`int Box_perp R`,
`int Box_perp R_nn`.

No previously authorized sector is omitted from the manifest.

## 2. Subtraction order is correctly frozen before projection

The contract requires:

1. unrenormalized F/M/G pole generation in general `d`;
2. bulk/ghost/local-composite subdivergence subtraction;
3. endpoint and lower-order geodesic counterterms;
4. full line-defect mixing renormalization, including simple and higher poles;
5. contact/polynomial separation only after renormalization;
6. genuine-defect projection at `u=0,1/2` before `d -> 4`.

This ordering prevents three invalid shortcuts:

- interpreting raw diagram poles as physical defect residues;
- throwing away local/contact pieces before they have canceled UV subdivergences;
- projecting in strict four dimensions before evanescent/pole products are controlled.

## 3. Corrected B1 bookkeeping

The specification incorporates the post-ITER117 correction to the earlier pole-only language.

The physical single-log coefficient is decomposed as

`B1_total = B1_direct + B1_defect`,

where:

- `B1_direct` collects the explicit noncontact logarithmic part of the fully renormalized F/M/G loop contribution;
- `B1_defect` is induced by running/mixing of the genuine line-defect couplings acting through their lower-order/finite insertion kernels.

The required output `beta_defect` keeps this RG information explicit.

Thus no raw `1/epsilon^2` coefficient or single diagram is identified with `B1_total`.

## 4. Both simple and higher poles are required consistency data

The manifest requires raw and subtracted coefficients of both

`1/epsilon^2`

and

`1/epsilon`.

This is necessary because overlapping geodesic/subdivergence structures may produce higher poles, while beta functions of renormalized defect couplings are determined by the appropriately subtracted pole mixing. Higher-pole consistency is therefore a renormalization check, not a substitute for the simple-pole/RG system.

## 5. General-d projector is applied at the correct stage

ITER123 gives the exact genuine-defect projector at

`u=0`, `u=1/2`,

with determinant `1/4` in general dimension.

ITER124 explicitly forbids taking `d=4` or isotropically averaging the tangent before pole extraction. This preserves ordinary `d`-dependent trace structures and avoids aliasing the tangent-traceless channel.

The projector is used only after basis reduction/subtraction, so bulk or redundant poles cannot be silently reinterpreted as genuine defect residues.

## 6. Gauge and field-redefinition checks are acceptance conditions

A numerical/symbolic `B1_total` is not physically accepted unless the complete result is independent of unphysical gauge choice or satisfies an equivalent BRST consistency condition.

Likewise the final coefficient must be invariant under the ITER120 redistribution generated by

`delta g_mn=kappa^2[a R_mn+b g_mn R]`

when the action and relational observable are transformed consistently.

Failure of either check is a **renormalization-basis failure**, not evidence for a physical coefficient.

## 7. Stopping rules are logically sound

If

`B1_total != 0`,

then ITER116's valid RG statement implies the `O(G^2)` separated correlator cannot vanish identically. Computing the finite constant `B0` is unnecessary for that binary question.

If

`B1_total = 0`

exactly, the finite `B0` calculation remains mandatory; a zero log coefficient does not imply a zero correlator.

If gauge/field-redefinition consistency fails, no physical interpretation is permitted.

## 8. Evanescent-operator caveat

The frozen contract correctly keeps the calculation in general `d` through pole projection. A future explicit loop implementation must still detect any genuinely evanescent tensor/operator structures that vanish at `d=4` but can multiply poles and feed finite or simple-pole data.

Such structures should be added as intermediate renormalization directions if encountered. Their possible existence does not invalidate the current physical genuine-defect projector, but it prevents assuming a purely four-dimensional tensor basis during raw loop reduction.

## 9. Reproducibility

The machine-readable manifest is checked by the dedicated validator for:

- all five sector categories;
- retained/genuine line bases;
- subtraction order;
- general-`d` projector points/determinant;
- required RG outputs;
- gauge, field-redefinition, decomposition, projector and target-independence checks;
- all three stopping rules.

The named CI artifact is present.

## Source classification

**`PASS_SCOPED_PROJECTED_RG_POLE_CALCULATION_SPEC_CLOSED`**

The remaining `B1` problem is now a finite projected renormalized loop calculation rather than an under-specified observable comparison.

## Highest-information successor

`ITER125_FIXED_GEODESIC_CURVATURE_PROJECTED_UV_TOPOLOGY_AND_MASTER_INTEGRAL_CENSUS`

Before evaluating residues, enumerate the minimal one-loop topologies and master-integral families required after the two-point projector is imposed, including line-parameter integrals and counterterm insertions. The goal is to determine which pieces can be delegated to ordinary massless bubble masters and which require genuinely new geodesic-line masters.

## Claim ceiling

Bridge credit remains **0**. Candidate theory remains **UNFORMED / 0%**. No `B1` value, noncancellation theorem, EDT fit, direct EDT/EFT conflict, bridge or new physics follows.