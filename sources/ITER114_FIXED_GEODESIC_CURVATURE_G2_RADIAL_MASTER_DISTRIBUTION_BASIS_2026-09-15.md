# ITER114 source authority — fixed-geodesic curvature `O(G^2)` radial master-distribution basis

Date: 2026-09-15
Gate: `ITER114_FIXED_GEODESIC_CURVATURE_G2_RADIAL_MASTER_DISTRIBUTION_BASIS`
Preregistration: `22f0ee94fb43aba82b52c9a8ba29c0d22c26ce52`

## 1. Fixed-geodesic one-loop source collapses to two nonlocal distributions plus contact

In the explicit one-loop fixed-geodesic matter calculation, the mixed and pure geodesic contributions are reduced by tensor identities and line-parameter integration to coefficients multiplying

`G_0(x,y)^2`.

Near four dimensions this singular distribution is renormalized into two nonlocal distributions plus contact terms. The source writes the final separated result in terms of

`H_0^(k)(x;mu) = i/(64 pi^4) partial^2 [ log^k(mu^2 x^2) / x^2 ]`,

with `k=1,2`, together with a local delta-function contribution.

The `H_0^(2)` term contains the source's unusual one-loop double logarithm generated by the double pole in dimensional regularization/geodesic renormalization.

Predicate A: **PASS**.

## 2. One-loop template has at most double logarithms

The 2018 renormalized fixed-geodesic result contains:

- single-log distribution `H_0^(1)`;
- double-log distribution `H_0^(2)`;
- local contact terms.

The double logarithm originates from the strongest pole structure encountered by the one-loop geodesic observable. Curvature insertions add derivatives and different tensor numerators, but do not add an extra loop integration or a new independent physical scale at the frozen `O(G^2)` order.

Within this one-loop/geodesic-renormalization template, the noncontact logarithmic degree is therefore bounded by two.

Predicate B: **PASS_SCOPED_TO_ONE_LOOP_TEMPLATE**.
`DOUBLE_LOG_DEGREE_CONTROL`: **PASS_SCOPED**.

## 3. Final anchored scalar correlator is radial

The background is flat Euclidean space and the observable is a scalar at each endpoint. The fixed geodesic introduces the separation vector `l^mu`, but after all tensor indices and the physical tangent direction `n^mu=l^mu/l` are contracted, there is no second external vector from which to form an independent angle.

Consequently the complete gauge-invariant anchored two-point function depends only on `l^2` (and the renormalization scale `mu`). Intermediate F/M/G tensors may depend on `n^mu`; that dependence must reduce to radial derivatives in the final scalar.

Predicate C: **PASS**.
`GEODESIC_DIRECTION_ROTATIONAL_SCALAR_CONTROL`: **PASS_CONTROL**.

## 4. Engineering dimension fixes the radial prefactor

In four dimensions,

`[R]=L^-2`, `[RR]=L^-4`, `[G^2]=L^4`.

Therefore any separated `O(G^2)` curvature correlator with no additional physical mass/length scale must carry

`G^2/l^8`

times a dimensionless function of `mu l` and dimensionless finite renormalization data.

Predicate D: **PASS**.

## 5. Curvature derivatives map the `H^(k)` template to `l^-8` log-polynomials

The matter fixed-geodesic distributions have separated scaling `L^-4` with single/double logarithms. A leading scalar-curvature insertion contains two derivatives of the metric. Relative to an undifferentiated endpoint field at each side, the `RR` scalar channel supplies four net external derivatives.

Acting with any rotationally contracted four-derivative operator built from `partial_mu` and `n_mu` on a radial function of the form

`l^-4 * P_2(log(mu^2 l^2))`

produces, away from `l=0`, another radial function of the form

`l^-8 * Q_2(log(mu^2 l^2))`,

where `Q_2` is a polynomial of degree no greater than two. Differentiation lowers powers of logarithms; it does not create a higher logarithmic degree.

Independent curvature vertices/loop numerators can change the coefficients and derivative contractions but, under the frozen massless one-loop assumptions, cannot create an additional dimensionless argument besides `mu l`.

Predicate E: **PASS_SCOPED**.

## 6. Complete noncontact ansatz

The most general separated anchored fixed-geodesic scalar-curvature correlator through `O(G^2)` compatible with the frozen source/one-loop structure is therefore

**`C_RR^geo(l;mu) = (G^2/l^8) [A_0(mu)+A_1(mu) L+A_2 L^2] + O(G^3)`,**

with

`L = log(mu^2 l^2)`.

Equivalent conventions may shift constant terms between `A_0`, `A_1` and the definition of `mu`; the physical `mu`-dependence must cancel against the running of finite observable-specific renormalization constants as in the 2018 precedent.

The coefficients `A_0,A_1,A_2` are **not** imported from the matter calculation and are not inferred from EDT.

Predicate F: **PASS_CONTROL**.
`MATTER_TEMPLATE_CURVATURE_COEFFICIENT_SWAP_CONTROL`: **PASS_CONTROL**.
`EDT_COEFFICIENT_FIT_CONTROL`: **PASS_CONTROL**.

## 7. Contact distributions are a separate basis sector

Terms proportional to `delta^(4)(l)` and its derivatives are required for renormalization and local EFT matching but vanish at separated points. They are not part of the radial noncontact ansatz above.

Predicate G: **PASS_CONTROL**.
`CONTACT_RADIAL_BASIS_CONTROL`: **PASS_CONTROL**.

## 8. Why no additional separated function is needed at this order

The line parameter is integrated over a fixed dimensionless interval `tau in [0,1]`. With a single endpoint separation `l`, massless propagators and no additional physical scale, those parameter integrations can generate numerical constants and the same `mu l` logarithms but no independent cross-ratio.

A non-polynomial function of a second dimensionless variable would require another external separation, angle, mass ratio or physical scale, none of which is present in the anchored two-point setup after rotational scalar projection.

Predicate H: **PASS**.

## Source classification

**`PASS_SCOPED_G2_NONCONTACT_RADIAL_BASIS_L8_LOG0_LOG1_LOG2`**

The unknown one-loop fixed-geodesic curvature calculation has now been reduced from a large tensor/line-integral sum to three separated radial coefficients:

`A_0`, `A_1`, `A_2`.

This is a functional-form result, not a coefficient calculation.

## Derived finite-window effective exponent

For

`C(l)=G^2 l^-8 P(L)`, `P(L)=A_0+A_1 L+A_2 L^2`,

the local log-log slope is

`p_eff(l) := d log|C| / d log l = -8 + 2 P'(L)/P(L)`.

Thus even a nonzero `O(G^2)` term can exhibit an apparent finite-window slope below `-8` if the logarithmic polynomial varies appreciably. A fitted `-10` over a finite lattice window is therefore not, by itself, equivalent to a true leading `l^-10` asymptotic law.

This observation is algebraic and is **not** used to fit the EDT data in this gate.

## Highest-information successor

Use renormalization-group consistency to determine which combinations of `A_1,A_2` are fixed by geodesic/composite anomalous dimensions and which coefficient(s) remain independent finite matching data. If `A_2` or `A_1` can be fixed without full integral evaluation, the calculation may reduce to a single finite constant.

## Claim ceiling

Bridge credit remains **0**. Candidate theory remains **UNFORMED / 0%**. No values of `A_0,A_1,A_2`, no cancellation/noncancellation theorem, no EDT fit, no direct EDT/EFT conflict, no continuum EDT, `BRIDGE_DERIVED`, or new physics follows.