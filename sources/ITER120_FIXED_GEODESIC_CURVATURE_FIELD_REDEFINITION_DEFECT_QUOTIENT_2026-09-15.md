# ITER120 source authority — fixed-geodesic curvature field-redefinition / defect quotient

Date: 2026-09-15
Gate: `ITER120_FIXED_GEODESIC_CURVATURE_FIELD_REDEFINITION_DEFECT_QUOTIENT`
Preregistration: `8759d566ad880178f1b59ff5996ac4489873f9b8`

## 1. First variation of geodesic length

For a unit-speed geodesic `x^mu(s)` with tangent `n^mu`, the first variation of its proper length under a metric variation is

`delta l = (1/2) int_0^l ds n^m n^n delta g_mn`

when the background path satisfies the geodesic equation and the endpoint convention is held fixed consistently. Path-shape variations do not add an independent first-order bulk term because the geodesic is stationary under such variations.

Predicate A: **PASS**.

## 2. Standard one-loop local metric redefinitions generate the two dimension-2 line operators exactly

Take

`delta g_mn = kappa^2 [a R_mn + b g_mn R]`.

Then

`delta l = (kappa^2/2) [a int ds R_nn + b int ds R]`.

Thus the two lowest-derivative line sectors isolated by ITER118 are precisely the variations of the geodesic observable induced by the two standard curvature-linear local metric field-redefinition directions.

Because `a` and `b` are independent, the generated subspace has rank two:

`span{int R, int R_nn}`.

Predicate B: **PASS**.

## 3. Endpoint curvature transforms simultaneously

A field redefinition is not a change of the path alone. The endpoint scalar curvature must also be expressed in the new field variable.

For a general infinitesimal metric change `h_mn=delta g_mn`, the linearized variation of scalar curvature about flat space is

`delta R = partial_m partial_n h^mn - Box h`

up to the conventional sign associated with raising the variation. Substituting the curvature-linear field redefinition and using the linearized contracted Bianchi identity gives an endpoint dimension-4 shift proportional to

`kappa^2 Box R`

(with coefficient determined by `a,b`; nonlinear curvature terms are beyond the linear sector relevant here).

Therefore removing/rearranging the line directions by field redefinition necessarily reshuffles endpoint/local curvature operators as well. ITER120 quotients the **combined theory+observable representation**, not the line basis in isolation.

Predicate C: **PASS_CONTROL**.
`FIELD_REDEFINITION_OBSERVABLE_CHANGE_CONTROL`: **PASS_CONTROL**.

## 4. The dimension-2 line directions are redundant representation/scheme directions

Local invertible field redefinitions do not create new physical degrees of freedom. In effective gravity they are routinely used to redistribute operators proportional to Ricci curvature / the Einstein equations among the action and composite observables.

For the fixed-geodesic correlator, consistency requires transforming both the action and the relational observable. Under that complete transformation, the pair

`int R`, `int R_nn`

does not represent two new independent physical defect couplings; it represents how the same geodesic observable is written in different local metric field coordinates, together with the induced endpoint shifts.

This does **not** mean their diagrammatic/pole contributions may simply be dropped. It means they can be quotiented as independent physical renormalization data after the compensating transformations are retained.

Predicate D: **PASS_SCOPED_REDUNDANT**.
`R_RNN_INDEPENDENT_COUPLING_CONTROL`: **PASS_CONTROL**.

## 5. The two transverse dimension-4 line operators are not generated by an O(kappa^2) local metric field redefinition

The remaining independent line operators are

`int ds Box_perp R`,
`int ds Box_perp R_nn`.

A local metric variation must be dimensionless. Since `kappa^2` has mass dimension `-2`, an `O(kappa^2)` local field redefinition can multiply curvature operators of mass dimension two, namely `R_mn` and `g_mn R`.

A variation containing `Box R_mn` or `g_mn Box R` has mass dimension four and would require an additional inverse-mass-squared coefficient. In the pure frozen one-scale EFT, using `l^2` would make the **theory field redefinition depend on the particular observable separation**, which is not a local field redefinition of the underlying theory. Using an extra physical scale is outside the gate.

A standard higher-derivative local redefinition can instead first appear with additional powers of the gravitational/EFT expansion (schematically `kappa^4 Box R_mn`), which is beyond the `O(kappa^2)` field-coordinate change relevant to the `O(G^2)` defect quotient.

Predicates E-F: **PASS / GENUINE DEFECT DIRECTIONS RETAINED**.
`HIGHER_ORDER_REDEFINITION_ORDER_CONTROL`: **PASS_CONTROL**.

## 6. Quotient dimension

ITER118 line space:

`V_line = span{int R, int R_nn, int Box_perp R, int Box_perp R_nn}`.

Field-redefinition redundant subspace at the frozen order:

`V_red = span{int R, int R_nn}`.

Therefore

**`dim(V_line / V_red) = 2`**,

with representative genuine defect directions

`int Box_perp R`,
`int Box_perp R_nn`.

Predicate G: **PASS**.

## 7. Target independence

No EDT datum is used to choose `a,b`, define the quotient or prefer a field-coordinate scheme.

Predicate H: **PASS_CONTROL**.
`EDT_FIELD_SCHEME_SELECTION_CONTROL`: **PASS_CONTROL**.

## Source classification

**`PASS_SCOPED_DIM2_LINE_SECTORS_FIELD_REDEFINITION_REDUNDANT_GENUINE_DEFECT_RANK_TWO`**

The physically independent curvature-specific line-defect renormalization problem is reduced from four allowed line directions to two at the frozen order, once ordinary local metric-field-coordinate redundancy is quotiented consistently with the endpoint observable.

## Consequence for leading-log calculation

The pole/running projection feeding `B_1` need not treat `int R` and `int R_nn` as independent physical matching data. Their contributions may be moved between action, geodesic and endpoint sectors by field redefinition and should cancel out of a consistently transformed physical result.

The genuinely new defect pole calculation can focus on the transverse higher-derivative representatives

`int Box_perp R`,
`int Box_perp R_nn`,

while retaining the redundant sectors as a strong field-redefinition consistency check.

## Highest-information successor

Compute the projected UV/pole mixing of these two genuine defect operators into the single-log coefficient `B_1`. If symmetry/Bianchi identities further reduce their projection to one combination, the noncancellation diagnostic becomes a one-number pole calculation.

## Claim ceiling

Bridge credit remains **0**. Candidate theory remains **UNFORMED / 0%**. No pole residue, no `B_1` value/noncancellation theorem, no EDT fit or direct EDT/EFT conflict, no `BRIDGE_DERIVED`, or new physics follows.