# ITER117 source authority — fixed-geodesic scalar-curvature counterterm completeness

Date: 2026-09-15
Gate: `ITER117_FIXED_GEODESIC_CURVATURE_COUNTERTERM_COMPLETENESS_AUTHORITY`
Preregistration: `e4a795628797db43b8487d8a916c666600382431`

## 1. What the known geodesic wave-function counterterm actually does

In the explicit fixed-geodesic matter calculation, the embedding-coordinate renormalization is written schematically as

`chi_B = Z_chi chi_R`,

with `delta Z_chi = O(kappa^2/l^2)` at one loop. Its contribution to the two-point function is the first variation of the **lower-order** observable under an endpoint displacement. For a scalar matter propagator this is source-explicit:

`G_CT ~ -i delta Z_chi l^mu partial_mu G_0(l)`.

Because `G_0(l)` is nonzero for separated points, this counterterm itself has a separated nonlocal contribution and participates in cancellation of geodesic UV divergences.

Predicate A: **PASS**.

## 2. For scalar curvature the lower-order separated correlator is contact-only

The 2026 low-energy curvature calculation rederives the tree-level result

`<R(x)R(y)>_tree`

as an analytic polynomial in external momentum. In position space it is a derivative of a four-dimensional delta function and therefore vanishes for nonzero source-sink separation.

Thus, for the anchored curvature observable,

`C_tree(l)=0` for `l != 0`

as a separated distribution.

Predicate B: **PASS**.
`MATTER_TREE_CURVATURE_TREE_SWAP_CONTROL`: **PASS_CONTROL**.

## 3. The scalar-style Z_chi counterterm is separated-point inactive for RR

Applying the same endpoint-coordinate renormalization to curvature gives, schematically,

`C_CT,Zchi(l) ~ delta Z_chi l^mu partial_mu C_tree(l)`.

Since `C_tree(l)` and all of its ordinary derivatives are contact distributions, this term remains supported at `l=0`. It cannot cancel or create a separated `l^-8` / `l^-8 log l` divergence for `l>0`.

This is a qualitative difference from the matter-scalar precedent.

Predicate C: **PASS**.
`CONTACT_NONLOCAL_COUNTERTERM_CONTROL`: **PASS_CONTROL**.

## 4. Ordinary local bulk/action and local endpoint counterterms do not automatically solve a separated geodesic divergence

The standard low-energy gravitational counterterms are local curvature invariants. In the 2026 master-coordinate curvature calculation, their contribution at the relevant order is polynomial in momentum and therefore contact in position space.

Likewise, a local endpoint renormalization whose lower-order insertion reduces to derivatives/multiples of the tree `RR` contact correlator remains contact at separated points.

Therefore a hypothetical **nonlocal separated UV divergence** generated by the fixed-geodesic M/G sectors cannot be declared renormalized by these local sectors without an explicit mixing calculation.

Predicate D: **PASS_SCOPED**.
`LOCAL_BULK_GEODESIC_LINE_COUNTERTERM_CONTROL`: **PASS_CONTROL**.

## 5. Independent renormalization of R and chi does not prove renormalization of the composite nonlocal product

The target observable is not merely `R_R(x) R_R(y)` evaluated after a passive coordinate shift. It contains a metric-dependent geodesic/worldline operator whose fluctuations are correlated with the endpoint curvature insertions.

General composite-operator logic does not guarantee that the product of separately renormalized factors is finite when their defining nonlocal structures overlap. The fixed-geodesic matter source is itself evidence of this phenomenon: fixing the fluctuating geodesic introduces ultraviolet divergences absent from the ordinary propagator and requires observable-specific renormalization.

For curvature endpoints, the source stack does not contain a theorem that `Z_chi` plus ordinary local curvature renormalization is complete. In particular, line-/endpoint-supported mixing compatible with the geodesic tangent and curvature can in principle appear and would have to be fixed by the actual divergence structure.

Examples of sectors that cannot be excluded a priori at the observable level include:

- endpoint curvature/tangent mixing such as relational contractions involving `R_mn n^m n^n` in addition to `R`;
- derivatives of endpoint curvature along the physical tangent;
- line-supported curvature insertions or endpoint-line composite terms generated by ultraviolet coincidence of metric fields on the geodesic;
- corresponding finite renormalization conditions.

This is an **allowed-basis warning**, not a claim that every listed sector is actually divergent at `O(G^2)`.

Predicate E: **PASS_OPEN_BASIS**.
`INDEPENDENT_OPERATOR_PRODUCT_RENORMALIZATION_CONTROL`: **TRIGGERS**.

## 6. Standard-basis sufficiency is not source-established

No frozen four-dimensional curvature-at-fixed-geodesic loop calculation exists, and the matter-scalar calculation does not establish a general nonlocal composite-operator renormalization theorem for arbitrary endpoint operators.

Therefore the project cannot assert that the scalar-style `Z_chi` is the only observable-specific counterterm required for `RR`.

Predicate F: **NOT ESTABLISHED**.

## 7. Minimal counterterm sectors that must remain in the calculation

Before interpreting a raw highest pole as the physical `B_1`, the calculation must allow and test at least:

1. **bulk/action sector:** ordinary gravitational EFT counterterms and gauge/ghost renormalization;
2. **local endpoint curvature-composite sector:** all local mixing allowed for the scalar-curvature insertion at the frozen derivative/order accuracy;
3. **geodesic embedding sector:** the known `Z_chi` renormalization, retained even though its direct action on the tree `RR` tail is contact-only;
4. **endpoint-line/nonlocal composite sector:** any additional line-/endpoint-supported counterterm actually required by M/G UV poles, to be classified by power counting, covariance/reparametrization structure and the physical tangent.

The fourth sector is the unresolved one. It should be generated by divergence analysis rather than guessed or fitted to lattice data.

Predicate G: **PASS_MINIMAL_SECTOR_CENSUS / EXPLICIT BASIS OPEN**.

## 8. Target independence

No EDT exponent, amplitude or fit window is used to select or normalize counterterms.

Predicate H: **PASS_CONTROL**.
`EDT_COUNTERTERM_FIT_CONTROL`: **PASS_CONTROL**.

## Source classification

**`PASS_SCOPED_ZCHI_SEPARATED_INACTIVE_CURVATURE_PRODUCT_COUNTERTERM_BASIS_OPEN`**

The strongest source-qualified conclusion is:

> the matter-scalar geodesic wave-function counterterm does not provide separated-point renormalization for `RR`, because the lower-order curvature correlator is contact-only. Independent renormalization of the local curvature operator and the geodesic embedding is not proven sufficient for their nonlocal composite product.

Thus ITER116's highest-pole diagnostic remains valid only **after** the curvature-specific line/endpoint counterterm basis is closed.

## Highest-information successor

Do not evaluate a numerical `B_1` residue yet. First perform an `O(G^2)` **superficial-degree / operator-mixing classification of line and endpoint divergences** for the fixed-geodesic `RR` observable. The goal is to determine whether additional nonlocal counterterms are actually allowed/required and to enumerate a finite basis if so.

## Claim ceiling

Bridge credit remains **0**. Candidate theory remains **UNFORMED / 0%**. No value of `B_1`, no noncancellation theorem, no direct EDT/EFT conflict, no continuum EDT, `BRIDGE_DERIVED`, or new physics follows.