# ITER090 source authority — fixed-volume Einstein-Hilbert minisuperspace identifiability no-go

Date: 2026-09-15
Gate: `ITER090_FIXED_VOLUME_EH_MINISUPERSPACE_IDENTIFIABILITY_NOGO`
Preregistration: `a5b68cad14d102824777d349be904bba94c19c34`

## Source formulas

arXiv `2408.07808` defines the Einstein-Hilbert running couplings

`G_k = g_k/k^2`,

`Lambda_k = lambda_k k^2`.

For the source's self-consistent Euclidean de-Sitter background it gives

`R_k = 3/sqrt(Lambda_k)`

and

`V_4(k) = (8 pi^2/3) R_k^4`

`       = (8 pi^2/3) * 81/(lambda_k^2 k^4)`.

The fixed-volume minisuperspace action, after introducing

`v_3 = V_3/V_4^(3/4)`, `s=t/V_4^(1/4)`,

is

`S = -(1/(24 pi)) [sqrt(V_4(k))/G_k] I[v_3]`,

where

`I[v_3] = integral ds [ dot(v_3)^2/v_3 + delta_0 v_3^(1/3) ]`,

with `integral ds v_3(s)=1` and source-fixed `delta_0`.

The source separately writes the fluctuation coupling

`g_eff^2(k) = 24 pi G_k/sqrt(V_4(k)) = (4/sqrt(6)) Lambda_k G_k approximately 1.63 lambda_k g_k`.

## Exact elimination of `k`

From the source-defined four-volume,

`sqrt(V_4(k))`

`= sqrt[(8 pi^2/3)*81] /(lambda_k k^2)`

`= (6 sqrt(6) pi)/(lambda_k k^2)`.

Using `G_k=g_k/k^2`,

`sqrt(V_4(k))/G_k`

`= [6 sqrt(6) pi/(lambda_k k^2)] / [g_k/k^2]`

`= 6 sqrt(6) pi/(g_k lambda_k)`.

Therefore the fixed-volume dimensionless Einstein-Hilbert minisuperspace action becomes

**`S[v_3] = - [sqrt(6)/(4 g_k lambda_k)] I[v_3]`.**

The RG scale `k` cancels exactly, and the two Einstein-Hilbert couplings occur only through the single product

**`p_k = g_k lambda_k`.**

Equivalently, the source's own `g_eff^2` is proportional to `p_k`.

## Identifiability consequence

Within the frozen action/truncation, the dimensionless fixed-volume probability law for the global minisuperspace variable has the schematic form

`P[v_3 | g_k,lambda_k] proportional to exp[-S[v_3;p_k]]`

subject to the fixed-volume constraint and the source's reduced-action assumptions.

Thus every statistic generated solely by this one-parameter reduced distribution — Gaussian covariance, Hessian eigenmodes, and higher action-derived cumulants — can at most constrain `p_k=g_k lambda_k` unless an additional coupling dependence enters through an observable/operator/measure outside the frozen action.

A larger number of moments does not increase the parameter rank of the family.

## CDT deformation parameter `omega`

The direct source's CDT effective action contains a deformation parameter related by

`delta/delta_0 = (omega_0/omega)^(8/3)`.

The source then rescales the CDT temporal/spatial convention to compare with the round continuum minisuperspace geometry. ITER078 already uses this deformation in the lattice-to-continuum parameter map.

Within the present two-coupling Einstein-Hilbert FRG truncation, `omega` is therefore not an additional independent FRG coupling. Counting it again as a second theory-space coordinate would mix a lattice anisotropy/geometric conversion parameter with the two EH running couplings under test.

## Predicate adjudication

- A running-coupling definitions: **PASS**.
- B self-consistent `V_4(k)`: **PASS**.
- C complete fixed-volume reduced-action prefactor: **PASS**.
- D exact `k` elimination: **PASS** — only `g_k lambda_k` remains.
- E action-generated-observable scope retained: **PASS_CONTROL**.
- F `omega` not double-counted as an FRG EH coupling: **PASS_CONTROL**.
- G higher cumulant independence test: **FAIL_FOR_INDEPENDENCE** within frozen action — no second coupling dependence is present.
- H variable-volume/local/higher-operator routes left outside scope: **PASS_CONTROL**.

## Scope caveats

This is not a theorem about the full quantum-gravity measure or arbitrary FRG truncations. A coupling-dependent functional measure, additional operators, local observables, non-minisuperspace modes, or variable-volume ensemble can introduce new parameter dependence.

The no-go is exact only at the source-defined fixed-volume Einstein-Hilbert minisuperspace action level used by the direct comparison.

## Source classification

**`PASS_SCOPED_ONE_PARAMETER_FIXED_VOLUME_IDENTIFIABILITY_NOGO`**

The ITER080 underdetermination is therefore structural within this reduced fixed-volume two-coupling sector: no number of additional cumulants of the same global variable can separate `g_k` and `lambda_k` if they are generated by the same frozen minisuperspace action.

Bridge credit remains **0**. Candidate theory remains **UNFORMED / 0%**.

## Consequence for successor selection

The next viable second-observable search must cross at least one boundary of the no-go:

1. **variable total four-volume / cosmological response**, exposing the conjugate cosmological direction;
2. **additional continuum operator** beyond Einstein-Hilbert minisuperspace;
3. **independently normalized local/non-minisuperspace observable** with source-defined FRG coupling dependence.

Reanalyzing fixed-volume `N_3(t)` moments alone is no longer a high-information route.
