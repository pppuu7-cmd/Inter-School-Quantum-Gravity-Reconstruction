# ITER112 source authority — fixed-geodesic scalar-curvature `O(G^2)` operator / diagram census

Date: 2026-09-15
Gate: `ITER112_FIXED_GEODESIC_CURVATURE_G2_OPERATOR_DIAGRAM_CENSUS`
Preregistration: `38e92177aec907b055be86c74239c2496dcd7937`

## 1. Perturbative closure is finite at `O(kappa^4)`

Use

`g_mn = delta_mn + kappa h_mn`,

`R = kappa R_1 + kappa^2 R_2 + kappa^3 R_3 + O(kappa^4)`,

and a fixed-length geodesic endpoint

`chi(1) = y + kappa chi_1 + kappa^2 chi_2 + O(kappa^3)`.

The 2018 fixed-geodesic calculation gives `chi_1` explicitly as a line integral of `h` and its derivatives along the background straight geodesic and supplies the corresponding second-order geodesic expansion. It states that, through one graviton loop, the fixed-geodesic observable decomposes into three source-distinct sectors:

1. ordinary field-theoretic corrections;
2. first-order endpoint/geodesic corrections correlated with first-order field-theoretic corrections;
3. pure second-order geodesic corrections.

This is a perturbative statement about the metric-dependent localization itself and is not specific to a matter-scalar insertion.

Predicate A: **PASS**.

## 2. Curvature evaluated at the fluctuating endpoint has a closed Taylor expansion

For a scalar curvature insertion at the endpoint,

`R(chi(1)) = kappa R_1(y)`
` + kappa^2 [R_2(y) + chi_1^a partial_a R_1(y)]`
` + kappa^3 [R_3(y) + chi_1^a partial_a R_2(y) + chi_2^a partial_a R_1(y)`
`                 + (1/2) chi_1^a chi_1^b partial_a partial_b R_1(y)]`
` + O(kappa^4)`.

For a two-point function whose other endpoint is used as the geodesic anchor,

`R(x) = kappa R_1(x) + kappa^2 R_2(x) + kappa^3 R_3(x)+...`.

Therefore no localization term above second order is needed to construct the correlator through overall `kappa^4 ~ G^2`.

Predicate C: **PASS**.

## 3. Complete calculation classes at `O(kappa^4)`

Expanding both the insertions and the Einstein-Hilbert/gauge-fixed path integral yields a finite census.

### Class F — ordinary curvature-field sector

These are the diagrams already present before changing the relational localization:

- `R_1-R_1` with one-loop graviton/ghost self-energy and associated counterterms;
- `R_1-R_2` / `R_2-R_1` with the interaction insertions required by perturbative order;
- Gaussian/tree contractions of `R_2-R_2`;
- `R_1-R_3` / `R_3-R_1` contractions;
- contributions generated by one quartic interaction or two cubic interactions acting on the leading `R_1-R_1` insertion;
- local gravitational EFT counterterm insertions required at the same loop order.

The 2026 master-coordinate calculation provides explicit curvature vertices and demonstrates that the complete field-plus-master-coordinate result is gauge independent. Its ordinary field-theory diagrams therefore provide curvature-specific vertex authority for this sector.

### Class M — mixed first-order geodesic / curvature sector

The first localization correction is

`L_1 R := chi_1^a partial_a R_1`.

At overall `kappa^4`, it enters through the finite possibilities generated by:

- a first-order geodesic correction combined with a second-order curvature insertion (`R_2 x L_1 R` and the anchored counterpart);
- a first-order geodesic correction combined with one cubic gravitational interaction acting on the leading curvature insertion structure;
- equivalent contractions in which one geodesic-line `h` is correlated with a field-theoretic graviton line.

This is the curvature analogue of the 2018 paper's explicitly drawn mixed geodesic/field-theoretic class.

### Class G — pure second-order geodesic sector

The complete second-order endpoint operator needed at this order is

`L_2 R := chi_2^a partial_a R_1`
`       + (1/2) chi_1^a chi_1^b partial_a partial_b R_1`
`       + chi_1^a partial_a R_2`.

Correlating `L_2 R` with the leading curvature insertion produces the pure localization class. It contains two graviton fields distributed along the geodesic/end-point correction and is the direct curvature counterpart of the 2018 pure second-order geodesic diagrams.

No `chi_3` term can enter before `O(kappa^5)` in a correlator with a leading `kappa R_1` insertion.

Predicates B-C: **PASS**.

## 4. Path-integral interaction order is also finite

Since the leading product `R_1 R_1` already carries `kappa^2`, reaching `kappa^4` requires at most two additional powers of `kappa` from the gauge-fixed gravitational action/ghost sector. Hence the required action expansion terminates at:

- one quartic interaction;
- or two cubic interactions;
- together with the corresponding ghost vertices and one-loop counterterms.

Terms with higher action vertices are beyond the frozen order. The calculation is therefore finite not only in localization order but also in bulk interaction order.

Predicate H (finite graph): **PASS**.

## 5. Two distinct renormalization sectors are mandatory

### Bulk/local curvature sector

The curvature insertions and gravitational action require the standard local EFT/composite renormalization. The 2026 master-coordinate calculation explicitly organizes the curvature correlator with the `O(R^2)` effective Lagrangian and finds that the higher-curvature low-energy constants contribute local/contact structures at the calculated noncoincident order.

### Geodesic/localization sector

The 2018 fixed-geodesic calculation finds additional UV divergences localized on the geodesic construction. They are not removed by ordinary bulk counterterms alone and require a wave-function-type renormalization of the geodesic embedding coordinates. The finite result contains new logarithmic structures.

These sectors cannot be identified with each other.

Predicate D: **PASS**.
`FIELD_GEODESIC_RENORMALIZATION_SWAP_CONTROL`: **PASS_CONTROL**.

## 6. Cancellation channels for the separated `G^2/l^8` tail

For the missing curvature calculation, possible cancellation of the complete `O(G^2)` separated tail can only be decided after summing **all noncontact pieces** from Classes F, M and G together with the finite parts induced by the geodesic renormalization prescription.

The following cannot by themselves cancel a separated tail:

- purely analytic local momentum-space terms whose Fourier transforms are contact distributions in the fixed observable;
- tadpoles that vanish in the adopted dimensional-regularization setup;
- terms removed only by setting the separation to coincidence.

By contrast, the 2018 precedent proves that mixed and pure geodesic sectors can survive at separated points and contribute logarithmic/nonlocal terms. They therefore participate legitimately in the noncancellation question.

Predicate E: **PASS_SCOPED**.
`CONTACT_TAIL_CANCELLATION_CONTROL`: **PASS_CONTROL**.

## 7. Gauge cancellation is global across classes

The 2018 calculation shows explicitly that the field-theoretic and geodesic pieces can each retain gauge-parameter dependence while the renormalized fixed-geodesic observable becomes gauge independent after all required classes and geodesic renormalization are combined.

Likewise the 2026 relational curvature calculation requires localization/coordinate corrections to obtain the physical gauge-invariant answer.

Therefore gauge dependence of any single Class F/M/G contribution is not a failure, and dropping a class because it is gauge dependent would invalidate the observable.

Predicate F: **PASS_CONTROL**.
`GAUGE_CLASSWISE_INVARIANCE_CONTROL`: **TRIGGERS**.

## 8. No target-driven class selection

No EDT value, exponent or fit window is used to decide which perturbative classes exist or to choose finite counterterms. The census follows solely from perturbative expansion and source-qualified renormalization structure.

Predicate G: **PASS_CONTROL**.
`EDT_TARGET_CLASS_SELECTION_CONTROL`: **PASS_CONTROL**.

## 9. Minimal calculation graph

A source-faithful `O(G^2)` fixed-geodesic curvature calculation can now be staged as:

1. generate `R_1,R_2,R_3` in the same perturbative conventions as the 2026 curvature calculation;
2. import/derive `chi_1,chi_2` in the same gauge conventions;
3. build `L_1 R` and `L_2 R` above;
4. enumerate Class F/M/G Wick contractions plus cubic/quartic/ghost interaction insertions through `kappa^4`;
5. separate contact from noncontact terms before Fourier transformation / final distance representation;
6. renormalize bulk curvature/action and geodesic embedding sectors independently;
7. verify cancellation of gauge parameters only in the complete sum;
8. extract the coefficient functions multiplying `G^2/l^8`, including `log(mu l)` and `log^2(mu l)` terms.

This list is finite. No resummation or infinite tower is forced at this order.

## Source classification

**`PASS_SCOPED_FINITE_G2_OPERATOR_DIAGRAM_CENSUS_CALCULATION_READY`**

ITER112 does not compute the coefficient. It establishes that the missing theory problem is a finite one-loop/composite calculation with a closed class structure and no source-level ambiguity about the existence of the geodesic correction sectors.

## Highest-information successor

Before evaluating tensor integrals, derive the **lowest-cost cancellation diagnostic**: determine which F/M/G contributions can generate the unique nonlocal momentum-space structure corresponding to `G^2/l^8` (and logs), and whether gauge/BRST identities fix any linear combination. This may prove noncancellation or reduce the number of integrals required.

## Claim ceiling

Bridge credit remains **0**. Candidate theory remains **UNFORMED / 0%**. No coefficient, cancellation/noncancellation result, direct EDT/EFT conflict, continuum EDT, `BRIDGE_DERIVED`, or new physics follows.