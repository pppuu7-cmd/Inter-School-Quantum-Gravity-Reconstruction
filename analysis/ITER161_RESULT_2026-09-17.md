# ITER161 terminal result — endpoint-selected open metric leg derived; complete pole tensor still BLOCKED

Date: 2026-09-17

Gate: `ITER161_G_B2S2_ENDPOINT_RAW_TENSOR_LIFT_TO_ITER118_OPERATOR_EQUATIONS`.

## Provenance

- Preregistration commit: `e004e8ff0d4977676b3e70c34a78c438965dc62a`.
- Initial pretrace structural producer: `e7be19d781e1121646913264cbe7f2da007f0ae0`.
- Preliminary Critic: `c1d5fbbfe7c2e7ea65908901bddbb1c33148d46b` — preserved but superseded in one argument before terminalization.
- Self-correction: `c19f407f48de41978c73c04df634a8deddf86785`.
- Endpoint-selected open-leg producer: `3b47cbeaa5ec3ed8d3023b8774a8f8799aac02db`.
- Final adversarial Critic: `168ed215e8cf9bb167d3cb6efaf633a58e1cb1c6`.
- Reproducible workflow head: `fc33a5b677ab608b2f46aaad019aceb5f03bf18b`.
- GitHub Actions run: `35250000806` — **success**.
- Artifact: `10509541616`, `iter161-g-endpoint-tensor-lift-diagnostic`.
- Artifact ZIP SHA256: `7a925baee50236385ed85936d7ffa8174da67bb7bed0de7100e3a331081ab5a5`.
- Exact pretrace diagnostic JSON SHA256: `40e7a5ff9f04cd1f0a83dda33ce0c1a6d015497e2e688f83a43ba9ce4688d6a8`.
- Exact endpoint-open-leg JSON SHA256: `556889d417ee71b89d7478c8513afe72aae171cb02fdfe55d0edc2214e2fc101`.
- Persisted copy of the open-leg result: `analysis/ITER161_OPEN_LEG_FACTORIZATION_RESULT_2026-09-17.json`, commit `f2d00da21f9e50fdf1e86911cafb8282eecff8fc`.

The GitHub artifact hash agrees with the GitHub artifact digest, and the workflow's own `iter161_sha256.txt` records the same two JSON hashes.

## 1. New structural result: the raw tensor information is not lost as early as ITER157/158 feared

The initial ITER161 producer retained the maximal post-`g2_real` pretrace object

`U[alpha,r,m,n]`,

where `alpha` is the `dR1` derivative label and `r,m,n` are `Gamma2` connection indices. Exact Fraction tests in `D=4,5,6` verify

`G1 = sum_{alpha,m,n} n_m n_n U[alpha,alpha,m,n]`.

The final scalar contraction has a nontrivial kernel, so the scalar `b^2*S^2` coefficient cannot be uniquely inverted back to this tensor. That anti-inversion result remains valid.

However, before terminalization an adversarial self-check found that the preliminary Critic had made an overstrong statement: opening one propagated graviton-side matrix is **not arbitrary once the affine endpoint geometry is used**.

## 2. Endpoint geometry uniquely selects the nonshrinking open leg

ITER143 freezes the G-family edge geometry:

- q edge: `tau L`;
- k edge: `(1-tau)L`.

Therefore:

- lower endpoint `tau -> 0`: q edge shrinks, retain the k-side metric leg;
- upper endpoint `tau -> 1`: k edge shrinks, retain the q-side metric leg.

No pole sign/value or ITER160 normalization is used to make this selection.

## 3. Exact upper-endpoint open metric vertex

The upper-endpoint producer keeps the q-side propagated source matrix `A_ab` arbitrary while contracting the shrinking k-side `dR1` source through the exact ITER140 `Gamma2` bilinear.

It constructs a unique symmetric matrix

`V_upper_ab(q,k,n;D)`

such that

**`G1 = sum_ab A_ab V_upper_ab`**.

This is a real source-faithful one-graviton-side tensor lift before scalar contraction.

Exact deterministic tests:

- `D=4`: `G1 = 165/2`, reconstructed `A:V_upper = 165/2`;
- `D=5`: `G1 = 60`, reconstructed `A:V_upper = 60`;
- `D=6`: `G1 = 665/16`, reconstructed `A:V_upper = 665/16`.

All open matrices are symmetric. The tensor structure was derived without consuming the ITER160 `-525/(pi^4 L^10)` endpoint residue.

Structural sub-classification:

**`PASS_STRUCTURAL_ITER161_ENDPOINT_SELECTED_OPEN_METRIC_LEG_FACTORIZATION`**.

## 4. Exact lower-endpoint open metric factorization

At `tau -> 0`, the q edge shrinks and the k-side propagated `R_mu,ab` leg is kept open. Source-faithfully there are symmetric matrices

`W_lower_mu,ab(q,k,n;D)`

such that

**`G1 = sum_mu,ab R_mu,ab W_lower_mu,ab`**.

The same `D=4,5,6` fixtures reconstruct the exact original `G1_value`.

The derivative label `mu` is deliberately retained. A single G graph does not source-faithfully prove that this vector-labelled lower object is already one of the scalar ITER118 endpoint directions; that would require an additional covariance/operator-mixing step, potentially involving other M/G structures.

## 5. Why this is still not an ITER118 tensor pole equation

The new `V_upper` and `W_lower` objects are **raw open-leg numerator tensors**, not complete endpoint pole tensors.

For example the upper object enters schematically as

`Integral dk exp[i(1-tau)L n.k] V_upper_ab(q,k,n;D) / K`.

The unseparated numerator contains both genuine `1/K` terms and terms where `K` cancels.

ITER151 freezes three nonzero G-family upper-endpoint K-cancelled contacts:

1. `K*S^2`, coefficient `-1/(2*d-4)`;
2. `a^2*K*S`, coefficient `(d-1)/(2*d-4)`;
3. `a*b*K*S`, coefficient `1/(d-2)`.

ITER153 establishes that these cancelled-propagator pieces are ambient point-supported delta derivatives whose ordinary canonical pullback to the geodesic is not authorized by the current source chain. Their pole values remain unknown/null, not zero.

Independently, ITER124 requires endpoint/line renormalization **before** contact/polynomial separation.

Therefore applying the ITER160 `-525/(pi^4 L^10)` TWO_PROPAGATOR support residue as the normalization of the complete local operator tensor would still skip the frozen renormalization step.

## 6. Exact minimal missing primitive

ITER161 narrows the blocker substantially. The free symmetric metric leg is now explicitly available.

The remaining single primitive is:

> a source-faithful **distributional endpoint R-operation/extension acting on the source-derived open-leg unseparated `G_R1_chi2_Gamma2_dR1` endpoint vertex before Q/K contact separation**, preserving the endpoint-selected free symmetric metric leg and orientation while assigning the complete local pole tensor, including the cancelled-propagator endpoint distributions.

Once that object exists, its pole tensor can be compared directly with the ITER122 one-graviton vertices for

`[R,S,DR,DS,BoxR,D2R,BoxS,D2S]`.

## 7. Final Critic

Final Critic verdict:

**`PASS_CRITIC_ITER161_OPEN_LEG_DERIVED_POLE_EXTENSION_BLOCKER_SOUND`**.

The preliminary Critic's claim that the open-leg choice is arbitrary is explicitly rejected and preserved only as a documented in-gate false start. Its anti-scalar-inversion and renormalization-order checks remain valid.

## Terminal classification

**`BLOCKED_SCOPED_ITER161_G_ENDPOINT_TENSOR_LIFT_EXACT_MISSING_PRIMITIVE`**

This is a materially narrower BLOCKED than ITER157:

- raw endpoint support is known (ITER160);
- endpoint-selected free symmetric metric leg is now known (ITER161);
- exact factorization back to G1 is verified in general-D fixtures;
- what is still absent is specifically the **renormalized distributional pole tensor on that open leg**.

No equation manifest is frozen because the pole tensor does not yet exist. Therefore no coefficient/rank/nullspace solve is authorized.

## Claim locks

`B1_total = UNAUTHORIZED`.

`BRIDGE_DERIVED = false`.

`NEW_PHYSICS_FOUND = false`.

Candidate theory remains `UNFORMED / 0%`.

ITER160 remains a valid scoped raw-support PASS; ITER161 does not reinterpret it as an endpoint counterterm coefficient.
