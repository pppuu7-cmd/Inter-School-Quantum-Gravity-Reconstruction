# ITER160 terminal result — independently confirmed raw G `b^2*S^2` endpoint support split

Date: 2026-09-17

Gate: `ITER160_FIRST_MG_G_B2S2_RAW_ENDPOINT_SUPPORT_CONFIRMATION`.

Preregistration commit: `43fb329bbc9079d0f2b9413b4644b2658da53829`.

Preregistration parent: `8211cd1143ff549d2788cab8000662db17570659`, which explicitly contains the exploratory/prediction-generating calculation. ITER160 is therefore a prediction-known confirmation gate, not a blind discovery gate.

Producer implementation commit: `6b6f91caffd7c41dec19a4635c74e7c7b56b0770`.

Independent Critic implementation commit: `f4a70ad87a7c5623ea15c696bb1a8f39f362f8f1`.

Workflow launch commit: `8c3602384704aef8b45d2a8af05f60d19320f69d`.

GitHub Actions run: `35247996587`, job `105292991900`. At terminal-result write time GitHub still reports this infrastructure run as `queued`; no runner has been assigned. Queue state is not reclassified as scientific BLOCKED/FAIL and is not used as evidence for PASS.

Persisted exact producer output: `analysis/ITER160_RESULT_2026-09-17.json`, commit `5bbefb76a001e6b16f1a9a53a07e43a32777c793`.

Persisted exact Critic output: `analysis/ITER160_CRITIC_2026-09-17.json`, commit `fcaa79c0e425ce7b8ba2358b6abcc4a09b4a4309`.

Exact local output SHA256 values from execution of the frozen algebra:

- producer JSON: `424d8cdd936de13bddf745754ed13edf5da04eeee90249a84be7c64595007535`;
- Critic JSON: `fe6903f1d4bc2fdc5d85526fb7cb34b53ccc6b0b046e363aaa7380824cbeff2b`.

The GitHub workflow independently pins `sympy==1.13.3`; the local exact check used the available SymPy 1.14.0 environment. Cross-version CI determinism is therefore still infrastructure-pending and is not silently asserted.

## Frozen upstream object

ITER150 had already established for the raw separated connected-cross TWO_PROPAGATOR sector

`G_R1_chi2_Gamma2_dR1 : b^2*S^2 -> -1050/(pi^4 L^10) * 1/epsilon`,

with affine factor

`B(-3+2 epsilon,-4+2 epsilon)`

and exact total Beta residue `35`.

ITER160 does not change that object. It only tests its endpoint-support decomposition under the preregistered local meromorphic extension.

## Producer — distributional derivation

Using

`Res x^(-m-1+2 epsilon) = (-1)^m/(2 m!) delta^(m)(x)`,

the lower endpoint has `m=3` and smooth factor `(1-x)^(-5)`, while after `u=1-tau` the upper endpoint has `m=4` and smooth factor `(1-u)^(-4)`.

The derived Beta residues are exactly

- lower endpoint: `35/2`;
- upper endpoint: `35/2`.

Their sum is `35`, exactly reproducing the frozen full Beta residue.

The corresponding frozen graph prefactor is

`-30/(pi^4 L^10)`,

so the raw simple-pole support pieces are

- lower endpoint: `-525/(pi^4 L^10) * 1/epsilon`;
- upper endpoint: `-525/(pi^4 L^10) * 1/epsilon`.

All ten preregistered producer checks pass. The post-jet local remainders are regular, with exact endpoint limits `70` and `56` in the normalized Beta-factor check.

## Independent Critic

The Critic does not reuse the delta-derivative distribution calculation. It splits the analytically continued integral at `z=1/2` and repeatedly applies the exact incomplete-Beta integration-by-parts recurrence

`B_z(a,b) = z^a (1-z)^b/a + (a+b) B_z(a+1,b)/a`

until the residual integral is ordinary and finite at `epsilon=0`.

It independently obtains

- lower residue `35/2`, with shifted finite residual `15/4`;
- upper residue `35/2`, with shifted finite residual `7/3`;
- exact sum `35`;
- graph residues `-525/(pi^4 L^10)` at each endpoint.

All twelve Critic checks pass.

Critic verdict:

`PASS_CRITIC_ITER160_RAW_ENDPOINT_SUPPORT_SPLIT_SOUND`.

## Terminal classification

**`PASS_SCOPED_ITER160_RAW_G_B2S2_ENDPOINT_SUPPORT_SPLIT_INDEPENDENTLY_CONFIRMED`**

This confirms a raw-support decomposition of the already-frozen ITER150 TWO_PROPAGATOR master under the frozen local meromorphic prescription.

It does **not** close slot 7.

## Immutable claim ceiling

The two `-525/(pi^4 L^10)` residues are **not** coefficients of the ITER118 endpoint basis and are **not** endpoint counterterms.

No ITER153 contact distribution is imported or set to zero.

No ITER123 genuine-line projector is used.

No statement is made that either raw endpoint piece survives the full ITER124 subtraction/mixing sequence.

`B1_total = UNAUTHORIZED`.

`BRIDGE_DERIVED = false`.

`NEW_PHYSICS_FOUND = false`.

Candidate theory remains `UNFORMED / 0%`.

## Scientific successor

The next useful gate is not another source grep. It must retain the endpoint-supported raw tensor structure before scalar contraction and ask whether the confirmed G-family endpoint residue can be mapped, with exact free-index/orientation/normalization data, onto the immutable ITER118 endpoint operator basis. The existing ITER140 arbitrary-integer-D indexed engine is the natural upstream reconstruction point; its final scalar invariant reconstruction must not be treated as an invertible tensor map.
