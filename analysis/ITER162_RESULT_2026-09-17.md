# ITER162 terminal result — frozen upper covariant span is source-faithfully false

Date: 2026-09-17

Gate: `ITER162_OPEN_G_ENDPOINT_DISTRIBUTIONAL_R_OPERATION_AND_POLE_TENSOR`.

## Terminal classification

**`SCIENTIFIC_FAIL_ITER162_FROZEN_TENSOR_COVARIANCE_OR_SUPPORT_IDENTITY_FALSE`**

This is a scientific falsification of a prospectively frozen structural identity, not an implementation failure and not a statement that the microscopic open tensor is absent.

## Frozen parent and claim ceiling

ITER161 remains frozen:

- `PASS_STRUCTURAL_ITER161_ENDPOINT_SELECTED_OPEN_METRIC_LEG_FACTORIZATION`;
- terminal parent `BLOCKED_SCOPED_ITER161_G_ENDPOINT_TENSOR_LIFT_EXACT_MISSING_PRIMITIVE`;
- upper source-faithful factorization `G1 = A_ab V_upper_ab` remains valid;
- lower source-faithful factorization `G1 = sum_mu,ab R_mu,ab W_lower_mu,ab` remains valid.

The failed ITER162 identity is narrower: the prospectively frozen **36-column** covariant generator is not complete for the already-derived source tensor `V_upper_ab`.

`B1_total = UNAUTHORIZED`.

`BRIDGE_DERIVED = false`.

`NEW_PHYSICS_FOUND = false`.

Candidate theory remains `UNFORMED / 0%`.

ITER118 operator matching remains unauthorized.

## Preregistered identity that failed

ITER162 prospectively froze `V_upper_ab` as a symmetric homogeneous momentum-degree-four tensor generated from the seven seeds

`delta_ab`, `q_a q_b`, `q_(a k_b)`, `k_a k_b`, `q_(a n_b)`, `k_(a n_b)`, `n_a n_b`

multiplied only by scalar monomials in

`Q=q^2`, `K=k^2`, `S=q.k`, `a=n.q`, `b=n.k`

with total explicit tangent count exactly two. Mechanical enumeration produced 36 columns.

The preregistration explicitly permits terminal

`SCIENTIFIC_FAIL_ITER162_FROZEN_TENSOR_COVARIANCE_OR_SUPPORT_IDENTITY_FALSE`

iff a prospectively frozen exact structural identity is contradicted by the source-derived tensor, and forbids repairing that identity post-outcome inside ITER162.

## Direct exact counterexample

The decisive test does not fit any coefficients.

Choose the frozen unit tangent frame

`n = e0`,

and momenta with

`q0 = k0 = 0`.

Then

`a = n.q = 0`, `b = n.k = 0`.

For any transverse open component `V_ij` with `i,j != 0`, **every one of the frozen 36 covariants vanishes exactly**:

1. `delta_ab`, `q_a q_b`, `q_(a k_b)`, `k_a k_b` have explicit tangent count zero, so the frozen rule forces their scalar multiplier to carry two tangent counts through `a,b`; those multipliers vanish when `a=b=0`;
2. `q_(a n_b)` and `k_(a n_b)` vanish on transverse free indices;
3. `n_a n_b` vanishes on transverse free indices.

Therefore the frozen 36-span predicts

`V_ij = 0`

identically on this exact submanifold.

The microscopic ITER161 source-derived tensor does not.

Researcher direct witness, workflow run `35267891590`, job `105359526262`:

- `D=4`, `q=(0,1,2,0)`, `k=(0,2,-1,1)`:
  - all 36 frozen columns vanish for every tested transverse symmetric component;
  - exact source values include `V_11=6`, `V_12=-3`, `V_13=3`, `V_22=3/2`, `V_23=-3/2`, `V_33=3/2`;
- a second independent `D=4` configuration again gives nonzero source values while all 36 columns vanish;
- a `D=5` configuration again gives nonzero source values while all 36 columns vanish.

The Researcher manifest records **18 exact nonzero counterexample components** across its three cases.

Classification emitted by the source-level witness:

`SCIENTIFIC_FAIL_ITER162_FROZEN_TENSOR_COVARIANCE_OR_SUPPORT_IDENTITY_FALSE`.

Artifact:

- ID `10517765160`;
- name `iter162-fast-n2-witness`;
- ZIP SHA256 `ce81455b9833ac95b5c70b48b28c075611d11ca90431675cbfee7c897c789704`.

No `-525` residue, scalar 28-invariant inverse, floating fit, contact-zero convention, or ITER118 solve is used.

## Independent direct Critic

Independent Critic, same workflow run `35267891590`, job `105359526273`, uses changed configurations and dimensions `D=4,5,6` and consumes no Researcher JSON.

It independently verifies:

- all frozen transverse covariants vanish on `n=e0`, `n.q=n.k=0`;
- the source-derived tensor has nonzero transverse components in **every** Critic case;
- examples include:
  - `D=4`: `V_11=9`, `V_12=-9`, `V_13=-9/2`, ...;
  - `D=5`: `V_11=5/2`, `V_12=-5`, `V_13=15/2`, ...;
  - `D=6`: `V_11=9`, `V_12=9/2`, `V_13=-9`, ... .

Critic verdict:

**`PASS_CRITIC_ITER162_DIRECT_N2_FROZEN_SPAN_FALSIFIER_CONFIRMED`**.

Artifact:

- ID `10517556166`;
- name `iter162-fast-n2-witness-critic`;
- ZIP SHA256 `429a1685c5cc7235c786bf2abb70d0d0b415b1c2d39c3364b0ccf54d26692996`.

This rules out a coefficient-fitting artifact: the contradiction exists before any tensor coefficient reconstruction.

## Diagnostic mechanism: omitted `n^2`-saturated tangent-neutral sector

The microscopic source contains two tangent vectors `n_m n_n`. They need not remain explicit as `a=n.q`, `b=n.k`, or free `n_a` indices; they can contract internally to

`n^2 = 1`.

That produces momentum-degree-four symmetric rank-two covariants whose two tangent vectors are saturated internally and hence leave a tangent-neutral tensor structure. The frozen generator excluded scalar `n^2` and required its tangent count to remain explicit in `a`, `b`, or free `n` indices.

This exactly explains why the frozen span vanishes for transverse components at `a=b=0` while the source tensor survives.

A diagnostic-only omitted sector has been enumerated as 15 `n^2`-saturated tangent-neutral candidate columns. It is **not** adopted as an ITER162 repair. Rank diagnostics may further characterize that complement, but the direct witness is already sufficient to terminalize the frozen gate.

## Reconstruction history and why the terminal result is not INVALID

Earlier implementation attempts exposed two non-scientific defects and were not terminalized:

1. arbitrary non-unit `n` held-outs violated the frozen `n^2=1` domain;
2. an eight-fixture axis-unit solve had rank 34 and was enlarged to a prospectively fixed 12-fixture pool, yielding exact design rank 36.

After those implementation defects were removed, the frozen 36 system still failed every overdetermined changed-`q,k` source held-out in `D=4..10`, even though:

- selected design rank was exactly 36;
- the fitted coefficient continuation was stable through held-out `D=9,10`;
- `n^2=1` was enforced exactly;
- no forbidden scalar input was used.

That failure motivated the direct no-fit witness above. The direct witness proves the problem is the preregistered structural span itself, not the fixture implementation.

## Scoped K-divisibility sub-result preserved

The independent exact quotient computation inside the frozen 36-space remains valid as a structural statement:

`PASS_STRUCTURAL_ITER162_COMPLETE_FROZEN_SPAN_TENSOR_K_DIVISIBILITY_KERNEL`.

Actions run `35266919815`, job `105356271332` found independently in `D=4` and `D=5`:

- frozen span dimension: 36;
- quotient map modulo `K=k^2` rank: 26;
- exact K-divisible kernel dimension: 10;
- the kernel is exactly the 10 explicit-`K` columns;
- no hidden K-divisible combination exists among the other 26 frozen columns.

Artifact `10517062876`, ZIP SHA256 `5bbd727348a117353062fb7b4d04e53277c831d96da5670b2d3f9953712af573`.

This theorem is **only about the frozen 36-space**. Because the source tensor is not contained in that space, it cannot be promoted to a complete source support split.

## Laurent / ambiguity / quotient status

The distributional Laurent/R-operation, complete local ambiguity basis `A_local`, pole-tensor space `P_open`, and quotient `P_open/A_local` are **not reached in ITER162**.

They are not assigned zero dimension or zero rank.

Terminal scientific failure occurs earlier in the frozen DAG: the source tensor violates the preregistered covariant span required before support/Laurent aggregation.

Accordingly:

- `LAURENT_OPERATION_STATUS = NOT_REACHED_AFTER_FROZEN_SPAN_SCIENTIFIC_FAIL`;
- `LOCAL_AMBIGUITY_DIMENSION = UNDEFINED_NOT_COMPUTED`;
- `POLE_TENSOR_DIMENSION = UNDEFINED_NOT_COMPUTED`;
- `QUOTIENT_RANK = UNDEFINED_NOT_COMPUTED`.

No contact contribution is interpreted as zero.

## Lower endpoint status

`W_lower_mu,ab` remains source-derived and retains the derivative label `mu`.

ITER162 does not scalarize it and does not assign it to an ITER118 direction. The upper frozen-span failure terminalizes the current gate before any lower scalar operator matching.

## Scientific frontier change

Before this iteration, the highest blocker was expected to be the distributional Laurent/extension operation.

The counterexample moves the frontier **earlier and more sharply**:

> the first exact missing ingredient is a complete prospectively frozen covariant tensor basis that includes internally saturated tangent contractions such as `n^2` before any source coefficient reconstruction, K-support classification, or distributional Laurent operation.

This is useful scientific information: the microscopic tensor exists, but the first ITER162 covariance ansatz was too restrictive.

## Next admissible scientific action

ITER162 is terminal and must not be repaired in-gate.

The next gate must be separately preregistered before inspecting repaired source coefficients. It should prospectively construct the complete degree-four symmetric covariant span allowing all contractions of the two tangent vectors, including `n^2`-saturated tangent-neutral structures, then:

1. prove basis completeness/rank under `n^2=1`;
2. independently reconstruct `V_upper_ab` with held-outs;
3. repeat exact full tensor K-divisibility on the **complete** source-containing span;
4. only then return to the distributional Laurent/R-operation, `A_local`, and quotient question.

No ITER118 matching is authorized by this result.
