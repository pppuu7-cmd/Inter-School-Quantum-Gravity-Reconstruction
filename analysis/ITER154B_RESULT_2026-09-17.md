# ITER154B result — corrected primitive-bubble proper-subgraph R-operation

Date: 2026-09-17

## Provenance

- Frozen parent: `a85276e5bf8a7518dc037a33d5eb8c9a02ca14f5`
- Preregistration: `c35e3a1d7a6a7c78ed0a02b376a7b899319d6be6`
- Implementation: `70cc1ae00e7b854eed9472b903d5c447a35af6cf`
- Independent Critic: `6e2d869d385820d731fc3c1484a12a920d11240a`
- Workflow / execution HEAD: `bda11f0c4430610ce619028042639a52b8a32e50`
- Actions run: `35153927336`
- Job: `104988716931`
- Artifact: `10470730440`
- Artifact ZIP SHA256: `8481cd17f9490e39878a86245a89ba8cdb28c9314a519b9dc0477a405df025b9`
- Result JSON SHA256: `049b6bb80f603feab787e318cf482fd6b5e809496ea2938241d5c69e898a33ea`
- Critic JSON SHA256: `3a9d492fa0818692d038bf3f170b2983fc63f3becfabe32d8565a6a618a9b362`

Both result and Critic were rerun in the same clean full-history job and were byte-identical.

## Classification

`PASS_SCOPED_SLOT6_FIRST_MG_PRIMITIVE_BUBBLES_NO_PROPER_SUBDIVERGENCES`

Critic:

`PASS_CRITIC_ITER154B_PRIMITIVE_BUBBLE_SUBDIVIDENCE_ZERO_SOUND`

All implementation checks A-J and Critic checks A-H are true.

## Scientific result

The repair gate preserves both frozen authorities rather than conflating them:

- ITER125: the loop-dependent M/G core is a two-propagator bubble with one independent loop momentum;
- ITER141: each scoped first-M/G connected-cross family has exactly two cross Wick propagators, no local/self-pairing in scope, and frozen endpoint strata.

At operator-loop level the whole graph is therefore the effective multigraph

`V_eff=2, E=2, C=1, b1=1`.

For each of the three families, the only strict proper nonempty edge subsets are the two single-propagator subsets. Each has `E=1`, `b1=0`; pinching either bubble edge destroys the closed loop. There is no self-energy insertion, same-site tadpole/self-pairing, `S3/S4` action vertex or ghost insertion inside this scoped connected-cross channel.

Therefore the forest of **strict proper ordinary bulk/local-composite subdivergences is empty** for:

- `M_R2_chi1_dR1`;
- `M_R1_chi1_dR2`;
- `G_R1_chi2_Gamma2_dR1`.

The graph-specific slot-6 operation is exactly

`R_sub G = G`

for these three cross-channel cores, with zero proper-subdivergence counterterm insertions.

This is a derived zero with a narrow meaning. It does **not** set the whole one-loop bubble divergence, endpoint divergence, seven ITER151 contacts, endpoint counterterms, line mixing or `B1_total` to zero.

## ITER154 falsification note

The immediately preceding ITER154 preregistration used the three-site geometric incidence Betti number as if it were the loop-momentum Betti number. ITER125 explicitly freezes a bubble loop core, so that representation equivalence is false. The old preregistered hypothesis is preserved as falsified; ITER154B is a separate prospectively frozen repair gate using the correct operator-level topology.

## DAG update

For the current three first-M/G connected-cross families, ITER152 slot 6 is now closed: there are no strict proper ordinary bulk/local-composite subdivergences to subtract before endpoint/line renormalization.

The next missing primitive is slot 7: divergent endpoint/geodesic counterterm coefficients in the immutable ITER118 endpoint basis. Slot 5 remains numerically open because ITER153 showed that the local endpoint distribution is fixed only together with endpoint/line renormalization. Slot 8 line mixing and slot 9 renormalized contact mapping remain open. `B1_total` remains unauthorized.

## Claim locks

No contact-pole zero, endpoint coefficient, line-mixing residue, renormalized mapping, terminal ITER124 output, noncancellation, bridge, new physics or candidate theory follows. `BRIDGE_DERIVED=false`, `NEW_PHYSICS_FOUND=false`, candidate theory remains `UNFORMED / 0%`.