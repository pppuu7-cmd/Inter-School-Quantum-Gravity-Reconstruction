# ITER165 preregistration — open-G endpoint meromorphic distribution-family hypothesis gate

Date: 2026-09-18

## Frozen input
Use only the source-complete ITER163 21-term unseparated open-G covariant ledger, its affine endpoint phases, and the source dimensional regulator d=4-2 epsilon. ITER164 is retained as BLOCKED: no source-qualified graph-level endpoint R-operation has yet been derived.

## Scientific question
Can the unseparated open-G endpoint object be embedded, without retuning or contact deletion, in a prospectively specified meromorphic family of distributions whose Laurent pole is well-defined modulo a completely enumerated local endpoint ambiguity?

## Required construction manifest
A candidate family is admissible only if it explicitly records: (1) ambient variables and affine endpoint coordinate; (2) test-function/domain space; (3) d=4-2 epsilon dependence; (4) transverse scaling degree for every singular sector; (5) pullback/restriction conditions from ambient to affine support; (6) free symmetric metric leg; (7) lower/upper orientation separately; (8) all K-divisible/cancelled-propagator contact sectors rather than setting them to zero; (9) the full local ambiguity span built from endpoint delta distributions and allowed normal derivatives with source-faithful tensor structures; and (10) a Laurent coefficient manifest before any ITER118 matching.

## Independent lanes
Lane A: constructive hypothesis producer. It must emit the complete manifest and symbolic power/scaling bookkeeping for all 21 source terms.

Lane B: completeness falsifier. Independently enumerate singular strata/local delta and normal-derivative counterterms allowed by scaling degree, tensor rank and orientation; attempt to exhibit an omitted ambiguity or an ill-defined pullback.

Lane C: contact/orientation critic. Verify that QK/KQ K-divisible sectors are retained as contact candidates and that lower/upper endpoint orientations are not identified before extension.

These lanes are independent until terminal artifacts exist. fail-fast must be false when executed together.

## Frozen outcomes
PASS_SCOPED_ITER165_MEROMORPHIC_FAMILY_HYPOTHESES_COMPLETE: all required hypotheses are source-faithful, all 21 terms are covered, pullbacks exist under the frozen assumptions, and independent falsifiers find no missing local ambiguity. This authorizes only a subsequent explicit Laurent/R-operation calculation; it is not a pole-tensor PASS.

BLOCKED_SCOPED_ITER165_NONUNIQUE_OR_INCOMPLETE_HYPOTHESES: a required source hypothesis is absent, a pullback/product is not justified, or the local ambiguity cannot be completely bounded from the source data.

SCIENTIFIC_FAIL_ITER165_FROZEN_HYPOTHESIS_CONSTRUCTION: the frozen construction makes a definite mathematical prediction that is contradicted by an independent exact check.

NUMERICAL_OR_INFRASTRUCTURE_FAIL: implementation/execution failure before scientific adjudication.

## Anti-retuning locks
No use of ITER160 -525 normalization to infer tensor coefficients. No inverse reconstruction from the scalar 28-invariant map. No dropping K-divisible contacts. No threshold/model/basis weakening after results. No convenient extension may be promoted to source physics merely because it yields a unique pole.

## Claim locks
Even PASS gives zero bridge credit by itself. ITER118 rank solve and B1_total remain unauthorized. BRIDGE_DERIVED=false; NEW_PHYSICS_FOUND=false; NEW_QG_THEORY_REQUIRED=false; ALL_KNOWN_SCHOOLS_FAIL=false; candidate theory remains 0 / UNFORMED.
