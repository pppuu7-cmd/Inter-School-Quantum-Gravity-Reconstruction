# ITER153 result — endpoint-contact distributional extension authority

Date: 2026-09-17

## Authoritative provenance

- Frozen parent `main`: `d44245c8a1387df197489e95296096c3f004451e`
- Preregistration commit: `bbcb9021e0245f3a506efa072d90cff992177126`
- Initial source-authority commit: `1792940bd0b2d89788b5be3855c71b497018aa64`
- ITER124 ordering authority repair: `8f5550d38a5eda4f61df465555c388d6e883af17`
- Premature-scaleless-collapse source control: `d4c795370b099564fe66618cd24d346633eabb46`
- Scientific implementation commit: `139fa221d61a9650b29c9bd140a2771d2efb8120`
- Independent adversarial Critic commit / execution HEAD: `f7efddec2fa1b1548d06a936fd122414526b7ec1`
- Workflow commit: `28fa5b64284f5a5260ca7531eb487de6231b3251`
- Successful Actions run: `35153011242`
- Job: `104985687348`
- Artifact: `10470051348`
- Artifact ZIP SHA256: `1cb8c2ef156e74ba6076dad776adf68f295df5dfebbf74494edd732f88d606b3`
- Exact ITER153 result JSON SHA256: `23fd1f191a7baf5bbab965678724d3d930e8297b9fadaedacde780cd5ebda8bf`
- Exact Critic JSON SHA256: `c24c9221f1b72a6d3c421d9bd5f53c52cd8ba99ff5aa1ce36490f33a0d496e75`

The scientific implementation and the independent Critic were both executed twice in the same clean full-history GitHub Actions job. The second result/critic payloads were byte-identical to the first (`cmp` succeeded), and the terminal payloads were preserved in the named artifact.

## Terminal classification

`BLOCKED_SCOPED_SLOT5_REQUIRES_NEW_DISTRIBUTIONAL_EXTENSION_OR_RENORMALIZATION_INPUT`

All implementation checks A-N are true. Independent Critic classification:

`PASS_CRITIC_ITER153_SCOPED_BLOCKER_LOGIC_SOUND`

All Critic checks A-M are true.

This is a scientific scoped blocker, not a physical FAIL and not evidence that any contact contribution is zero.

## New mathematical data produced by ITER153

The seven immutable ITER151 endpoint contacts were reconstructed without changing their support, coefficient, endpoint or parent numerator. After cancelling the shrinking propagator factor, the exact polynomial degrees in the cancelled momentum are

`[1, 1, 2, 2, 2, 1, 2]`

for contacts 1-7 respectively.

Thus each cancelled-momentum Fourier factor is an ambient point-supported derivative of `delta^(4)` of order one or two, contracted with the surviving momentum and tangent tensors. In physical `d=4`, the corresponding ambient scaling degrees are therefore five or six and the ambient singular orders are one or two.

### Method A — microlocal pullback test

For an ambient delta derivative,

`WF(T) = {(0,xi): xi != 0}`

at the relevant point-supported singularity. For the frozen geodesic line map

`F(s)= +/- L s n`,

the normal set contains the nonzero transverse covectors satisfying

`n.xi = 0`.

An explicit exact transverse witness exists in every case. Hence `WF(T)` meets the line normal set for all seven contacts. The ordinary Hörmander sufficient criterion therefore does **not** license a canonical pullback of any of the seven ambient contact distributions to the one-dimensional geodesic.

This is deliberately not promoted to a theorem that every generalized renormalized restriction is impossible.

### Method B — independent transverse-mollifier diagnostic

Using a smooth four-dimensional approximate identity and restricting it to `x=L s n` produces the codimension-three factor

`~ eta^(-3)/L`

already for the undifferentiated ambient delta; the derivative contacts are at least as singular. The exact longitudinal contractions for all seven frozen numerator structures are nonzero, so this divergence is not removed by a trivial tangent projection.

This independently shows that an extra local renormalization datum is required before the endpoint restriction has a regulator-independent distributional value. The mollifier is a diagnostic only and was not used as a numerical prescription.

### Premature scaleless-zero control

A tempting shortcut was explicitly tested and rejected: after cancelling `Q` or `K`, one cannot first replace the frozen endpoint phase

`exp[+/- i L s (n.p)]`

by one, call the remaining momentum integral scaleless in dimensional regularization, and conclude that the contact is zero. For `s != 0` the phase is nontrivial; its Fourier transform is precisely the delta-derivative distribution whose endpoint pullback is under examination. Setting `s=0` first therefore assumes the disputed pullback and changes the mathematical object.

The evaluator contains a dedicated negative fixture for this error and rejects it for the correct reason.

## Frozen ITER124 ordering closes the loophole independently

The parent ITER124 specification requires the following order:

1. generate unrenormalized F/M/G poles in general `d`;
2. subtract bulk ghost and local-composite subdivergences;
3. add endpoint and lower-order geodesic counterterms;
4. renormalize the full line-defect mixing matrix including simple and higher poles;
5. **only then** separate contact/polynomial structures;
6. project the genuine-defect poles.

Therefore even an algebraically local/contact-looking raw term cannot be assigned its final renormalized endpoint coefficient by deleting it before the graph-level R-operation. The ITER151 seven-contact manifest is a valid raw/support decomposition, but it is not an independently renormalized numerical contact sector.

## Exact minimal blocker

ITER153 sharpens ITER152 slot 5 to the following concrete missing primitive:

> a source-qualified **graph-level R-operation / renormalization map on the unseparated first-M/G amplitude**, including the actual bulk/local-composite subdivergence subtractions and the endpoint/line renormalization dependencies that ITER124 requires before the contact/polynomial split.

This is stronger information than “distributional authority is missing”. The seven raw contacts, their derivative orders, singular support, line geometry and failure of the ordinary canonical pullback are now explicit. What remains undetermined is the local endpoint distribution selected by the full renormalization operation.

Accordingly, for all seven contacts the terminal machine payload records

- `one_over_epsilon2 = null`;
- `one_over_epsilon = null`;
- `absence_is_zero = false`.

No ITER126 prototype residue was imported.

## Dependency-DAG update

ITER152 slot 5 is **not closed numerically**. However ITER153 shows that the next highest-information primitive is not another generic distribution audit. It is the slot-6 graph-specific subtraction/R-operation gate on the unseparated first-M/G graphs.

The successor should prospectively freeze the exact graph/subgraph census, the bulk/local-composite counterterm inputs and forest/R-operation order, derive the actual subtraction map and graph-specific values where source-qualified, and keep endpoint/line coefficients symbolic until their own gates close.

The successor must not form `B1_total` and must not replace any of the seven contacts by zero before the frozen ITER124 renormalization order permits the contact split.

## Claim locks

- No renormalized contact pole value is claimed.
- Slot 5 remains numerically open.
- Slot 6 graph-specific subdivergence subtraction remains open.
- Endpoint counterterm coefficients, line-defect mixing and renormalized contact mapping remain open.
- All six ITER124 terminal outputs remain unauthorized.
- Full first-M/G noncancellation remains unauthorized.
- `BRIDGE_DERIVED = false`.
- `NEW_PHYSICS_FOUND = false`.
- Candidate theory remains `UNFORMED / 0%`.
- Bridge credit remains `0`.