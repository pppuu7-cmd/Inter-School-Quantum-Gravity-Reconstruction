name: ITER140 first M/G general-d rank-repair v6
on:
  push:
    branches: [main]
    paths:
      - 'analysis/iter140_family_dimension_shard_v6.py'
      - 'analysis/iter140_aggregate_v6.py'
      - 'analysis/iter140_d4_terminal_authority_manifest.json'
      - '.github/workflows/iter140_first_mg_general_d_invariant_continuation_v6_rank_repair.yml'
  workflow_dispatch:
jobs:
  family-dimension-shards:
    strategy:
      fail-fast: false
      matrix:
        D: [3,4,5,6,7,8,9,10]
        family:
          - M_R2_chi1_dR1
          - M_R1_chi1_dR2
          - G_R1_chi2_Gamma2_dR1
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install sympy
      - name: Exact frozen family x dimension shard with rank-path repair
        run: python analysis/iter140_family_dimension_shard_v6.py ${{ matrix.D }} ${{ matrix.family }}
      - uses: actions/upload-artifact@v4
        with:
          name: iter140-v6-D${{ matrix.D }}-${{ matrix.family }}
          path: iter140_v6_D${{ matrix.D }}_${{ matrix.family }}.json
          if-no-files-found: error
  aggregate:
    needs: family-dimension-shards
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install sympy
      - uses: actions/download-artifact@v4
        with:
          pattern: iter140-v6-*
          path: iter140_v6_shards
          merge-multiple: true
      - name: Frozen ITER140 v6 exact aggregate
        shell: bash
        run: |
          set -o pipefail
          python analysis/iter140_aggregate_v6.py iter140_v6_shards | tee iter140_v6.log
      - uses: actions/upload-artifact@v4
        with:
          name: iter140-first-mg-general-d-invariant-continuation-v6
          path: |
            iter140_first_mg_general_d_invariant_continuation_v6.json
            iter140_v6.log
          if-no-files-found: error
