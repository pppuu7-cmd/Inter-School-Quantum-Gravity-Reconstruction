# ISQGR recovery frontier addendum — active ITER140 and ITER146

Date: 2026-09-15
Repository: `pppuu7-cmd/Inter-School-Quantum-Gravity-Reconstruction`

This addendum supersedes stale iteration pointers in `recovery/state.json` for the current original fixed-geodesic curvature branch. Candidate theory remains **UNFORMED / 0%** and bridge credit remains **0**.

## ITER144 — terminal

`PASS_SCOPED_CUBIC_GRAVITON_S3_TENSOR_STRUCTURE_SOURCE_AUTHORITY_GLOBAL_PHASE_OPEN`.

Pinned FeynGrav cubic-graviton source structure is authorized prospectively. The source contains 231 parsed terms of momentum degree two, satisfies all leg and within-pair symmetries, and agrees with an independent Einstein-Hilbert Gamma-Gamma cubic reconstruction up to one constant nonzero factor `-2`. Absolute Euclidean action/Feynman-rule phase/sign normalization remains open.

## ITER145 — terminal

`PASS_SCOPED_M3_COLLINEAR_THREE_DENOMINATOR_REDUCES_TO_BUBBLES_ROUTING_CLOSED_NUMERATOR_OPEN`.

For `p=-Q-(1-tau)k`, `r=Q-tau k`, the three denominators

- `D1=[Q+(1-tau)k]^2`,
- `D2=k^2`,
- `D3=[Q-tau k]^2`

obey exactly

`tau D1 + (1-tau) D3 - tau(1-tau) D2 = Q^2`.

Hence the apparent generic-interior triangle denominator reduces algebraically to three bubble terms; no independent triangle master is required for this collinear routing. The S3 tensor numerator was deliberately left open.

## ITER140 — active heavy lane

Gate: first M/G general-d invariant continuation.

The optimized retry is active in GitHub Actions run `34991523078`, job `104457100505`. Two previous attempts established a reproducible infrastructure/runtime bottleneck; the current workflow changed only the timeout from 60 to 180 minutes after the 60-minute cancellation. Frozen scientific predicates remain unchanged.

Do not infer a scientific failure or PASS until this run terminates and its exact output is adjudicated.

## ITER146 — active independent lane

Gate: `ITER146_FIXED_GEODESIC_CURVATURE_M3_EXACT_NUMERATOR_SLICE_AUTHORITY`.

Preregistration commit: `01c829aab04c10a2cbfbd209a944f8b502f1d39e`.
Implementation commit: `79b29b606a0acc7a1bfe08b37da02a3bbc5f6383`.
Workflow launch commit: `229947b559e3738180282b77a59d2a81772ef0e2`.
Active run: `34994417063`, job `104466939263`.

ITER146 combines the authorized ITER144 S3 tensor structure with the exact ITER145 routing and explicitly contracts:

- R1;
- bulk chi1;
- derivative R1;
- three D=4 de Donder propagator numerators;
- the pinned cubic S3 tensor.

It reconstructs exact affine-loop-slice numerator polynomials by rational interpolation on four deterministic panels and tests withheld lambda values, degree `<=8`, all S3 leg permutations, routing conservation and global momentum-flip parity. This is the first direct interaction-dressed M3 numerator-allocation calculation in the branch.

## Current next action

1. Consume and adjudicate ITER146 as soon as its current run terminates; if PASS, promote from slice authority to a full invariant polynomial/reduction gate.
2. Independently consume ITER140 when it terminates; do not hold ITER146/M3 progress behind ITER140 because the two lanes are algebraically independent.
3. No B1, noncancellation, EDT comparison, bridge, new physics or candidate-theory claim is authorized before the complete projected pole system closes.
