# Current front — ISQGR

Date: 2026-09-14.

Candidate theory: **UNFORMED / 0%**. Overall programme roadmap readiness: **50%**. Bridge credit remains zero.

## Persistent locks

RC006 numerical retry remains unauthorized. RC009 remains scoped blocked on its tested reduced isotemporal route. Lorentzian Delta4 negative results remain preserved. RC008 corrected crossing remains source-blocked on an explicit incidence-aware global physical measure. BH004/BH004B remains source-blocked on a source-faithful quantum amplitude/refinement transport. No full EPRL/FK refinement, Lorentzian refinement, q-deformed bridge derivation or candidate-theory construction is authorized. Forbidden claims remain `ALL_KNOWN_SCHOOLS_FAIL`, `NEW_QG_THEORY_REQUIRED`, `NEW_PHYSICS_FOUND`, `BRIDGE_DERIVED`, `FULL_QG`, `UNIVERSAL_COMMON_PARENT_FOUND`.

## Established typed structure

The former atlas coordinate `composition/refinement` remains split into at least `C_seq` (CDT temporal/state-sum composition), `C_tensor` (HaPPY tensor/isometric contraction) and `C_scale_flow` (FRG/EAA theory-space scale flow). ITER068/070 rejected their strong physical identification.

The observable-first branch established a common spectral-dimension definition (ITER071), CDT-internal diffusion/lattice scale calibration (ITER072), and then localized the missing independent physical-resolution normalization (ITER073-074).

ITER075-077 showed that de-Sitter/minisuperspace and constant-curvature structures recur across CDT/FRG but are not, in the older frozen stacks, enough to identify the physical effective actions or curvature observables.

## ITER077 — terminal curvature-observable identity

Preregistration `f15c7e53d241533d371416b3c748fbf5d3748d7e`; source authority `38b58bc9a430b3c9fde0c5c2280f5493ebbb6dfd`; critic `cc65ae82a9f4a0376ee3ef5462d4b662bb2153dd`; terminal result `1c63326a22efeada1b7fb99c9163f04708d48c2b`.

**`FAIL_SCOPED_CURVATURE_OBSERVABLE_IDENTITY_REJECTED`**.

Retained positive residual: **`COMMON_CONSTANT_CURVATURE_STRUCTURE_ONLY`**.

CDT quantum Ricci curvature is an operational finite-radius metric-space observable built from distances between geodesic spheres and ensemble averaging. The frozen FRG objects are curvature-dependent EAA/background quantities and scale-dependent self-consistent de-Sitter effective geometries. No source-defined `delta <-> k`, ensemble/background or finite-radius-QRC/background-Ricci map is present.

## ITER078 — terminal direct CDT↔FRG reduced-action/parameter crosswalk

Preregistration `83b414e729cc18033086230fc6833285cf21bace`; source authority `0474d9756d23f225e7815aaa58311183dd34b853`; critic `af57c29b4ff879afc389520af7765ce57596d06e`; terminal result `de6aee8b075c8f794d4d3f187e62034ee41a224c`.

**`PASS_SCOPED / DIRECT_REDUCED_ACTION_PARAMETER_CROSSWALK_RG_SCALE_OPEN`**.

This is the strongest positive CDT↔FRG structural result so far. The prospectively frozen 2024 direct-comparison literature gives an explicit reduced minisuperspace map

`(N_4, omega, Gamma) -> (V_4(k), omega_0, G_k)`

with

`V_4(k) = (omega_0/omega)^(4/3) N_4 a^4`,

`24 pi G_k = (omega/omega_0)^(4/3) Gamma a^2`,

and

`omega^2 Gamma/(omega_0^2 sqrt(N_4)) ~= 1.63 lambda_k g_k`.

The direct source retains the CDT deformation `omega/omega_0`, introduces the corresponding reduced temporal scaling, and explicitly treats the opposite minisuperspace sign through a conformal-factor rotation prescription.

Hard ceilings remain:

- this is a **reduced minisuperspace/effective-action** crosswalk, not full CDT/EAA equality;
- the clean coupling relation is principally to the combination `lambda_k g_k`, not a global coordinate isomorphism of theory spaces;
- the putative CDT UV scaling path is source-constructed but not proven;
- bridge credit remains zero.

## ITER079 — terminal CDT correlation-length / FRG-scale authority

Preregistration `87c23a0ff5dcc134b7b98e588951c58f0c158b07`; source authority `46657aa21bbd191e6133171d4a2d9dd8fb402fff`; critic `4442eb47da3f52c72a4ede254b219fea8408e0e9`; terminal result `d8e632e5bd7522581b87d830acceef3f311e5746`.

**`PASS_SCOPED / REDUCED_SCALE_RELATION_ONLY`**.

Mandatory lock:

**`OPERATIONAL_CDT_XI_TO_FRG_K_MAP = NOT_ESTABLISHED`**.

The direct stack source-qualifies:

- a gravitational/geometric finite-size correlation length `xi_CDT ~ N_4^(1/4)` in the reduced 4D CDT setting;
- `xi_CDT a ~ V_4^(1/4)` in the de-Sitter finite-size description;
- FRG `k` as a regulator/mode coarse-graining scale with flow dominated by modes of momenta of order `k`;
- nontrivial near-UV relations among `a`, `a_t`, `xi`, `k` and the fixed reduced coupling `lambda_k g_k`.

But the authors explicitly say the precise `xi <-> k` connection is not known. The follow-up calls `a proportional_to 1/k` **only a dimensional relation**. The source-defined physical content is instead continuum scaling: for fixed `k` / fixed reduced coupling, `a -> 0` while `xi -> infinity`.

Direct CDT/FRG critical-exponent equality therefore remains unauthorized.

## New structural fact at the active front

The CDT↔FRG programme has now separated four levels that must not be conflated:

1. common reduced de-Sitter/minisuperspace language;
2. **explicit equation-level reduced action/parameter crosswalk — established in scope**;
3. reduced continuum scaling among lattice cutoff, geometric correlation length and FRG parameter — established in scope;
4. independently operational common RG resolution and full microscopic theory identity — **not established**.

The remaining scale blocker cannot be closed by manipulating the same reduced volume/action data again without circularity.

## Exact next scientific gate

**`PREREGISTER_ITER080_CDT_FRG_OUT_OF_SAMPLE_SPECTRAL_PREDICTION_FROM_REDUCED_MAP`**

The successor must use a held-out observable. Spectral dimension is the highest-information candidate because ITER071 already source-qualified the same abstract observable definition across CDT and QEG/FRG, while ITER078 now supplies a direct reduced parameter map that did not exist during the earlier spectral work.

The gate must forbid fitting the FRG trajectory, scale or normalization to the target CDT spectral curve. It should ask whether the independently established reduced map plus source-fixed information makes any falsifiable spectral prediction or parameter-free/held-out constraint. If no independent scale/trajectory can be fixed before looking at `D_s`, the result should be source-blocked rather than another target fit.

## Readiness

ITER077: **100% terminal FAIL_SCOPED; constant-curvature residual retained**.
ITER078: **100% terminal PASS_SCOPED direct reduced-action/parameter crosswalk; RG scale open**.
ITER079: **100% terminal PASS_SCOPED reduced scale relation; operational `xi <-> k` absent**.

Overall scientific programme: **50% (Δ0)**. Candidate theory: **0% / UNFORMED**. Bridge credit: **0**.
