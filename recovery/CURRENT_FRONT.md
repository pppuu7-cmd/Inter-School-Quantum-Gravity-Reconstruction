# Current front — ISQGR

Date: 2026-09-14.

Candidate theory: **UNFORMED / 0%**. Overall programme roadmap readiness: **50%**. Bridge credit remains zero.

## Persistent locks

RC006 numerical retry remains unauthorized. RC009 remains scoped blocked on its tested reduced isotemporal route. Lorentzian Delta4 negative results remain preserved. No full EPRL/FK refinement, Lorentzian refinement, q-deformed bridge derivation or candidate-theory construction is authorized.

Forbidden claims remain `ALL_KNOWN_SCHOOLS_FAIL`, `NEW_QG_THEORY_REQUIRED`, `NEW_PHYSICS_FOUND`, `BRIDGE_DERIVED`.

## RC008 valid source stack and invalidated numerical reconstruction

ITER061 remains **SCIENTIFIC PASS** for companion refinement-complex/gluing source authority.

ITER062A remains **SCIENTIFIC PASS — `RC008_RESTRICTED_AMPLITUDE_KERNEL_SOURCE_CLOSED`** for the restricted Riemannian EPRL-FK quantum-cuboid kernel/source chain.

ITER062B historical workflow PASS remains superseded by manual audit: the primary crossing used an unconverged `alpha=0.50` endpoint and the product of one-cell `Delta_FP` factors was not source-qualified as the global glued-complex reduced measure. Current status: `INVALID_SCIENTIFIC_INFERENCE_SOURCE_MEASURE_MAPPING_UNRESOLVED`; no gate credit.

ITER063 remains terminal `INVALID_IMPLEMENTATION_OR_SELECTOR_SYMMETRY_FAIL`; no gate credit.

ITER064 remains terminal `INVALID_DIAGNOSTIC_INFERENCE_CROSSING_ENDPOINT_NONCONVERGED`. Its only retained structural observation is that the earlier spatial-permutation discrepancy was seed/QMC-sensitive.

## ITER065 — terminal scoped source block

Frozen gate: `RC008_GLOBAL_GEOMETRIC_MEASURE_TOPOLOGY_AUTHORITY`.

Preregistration commit: `e1f97fa2ac91a48e9bb479c81074ea091c0d6d21`.

Initial production head: `3609580257c5ff35bbf292d7d04754031bf3523f`; run `34877639682`.

The geometric lanes completed successfully, but all four exact-PDF source lanes initially failed only because their Python environment omitted a required dependency. The initial aggregate therefore correctly classified `INFRASTRUCTURE_FAIL_SOURCE_EXTRACTION` and gave no scientific credit.

A minimal dependency/transport-only repair was committed at `d6a5e018d2029b3d629142d3b9bc144c09b9fe53`. Repair run `34877767148` successfully extracted all four frozen source IDs: `1508.07961`, `1605.07649`, `1701.02311`, `1804.00023`. The repair aggregate only nominated source passages and explicitly required manual equation-level audit.

### Geometric diagnostics

The frozen global geometric embedding diagnostics pass numerically:

- `coarse2`: rank `1/1` at all fixtures, finite Gram determinant, analytic-vs-finite-difference Jacobian disagreement <= `1.56e-10`;
- `fine32`: rank `6/6` at all fixtures, finite Gram determinant, analytic-vs-finite-difference Jacobian disagreement <= `1.50e-10`.

These are geometric pullback diagnostics only; they do not define the physical spin-foam measure.

The adversarial comparison of the old product of local one-cell `Delta_FP` factors against `sqrt(det(J^T J))` is nonconstant:

- coarse2 ratio CV `0.01783571272665122`;
- fine32 ratio CV `0.9369701593382911`.

Thus the old local-product prescription cannot be treated as merely the same pullback measure times an irrelevant normalization.

### Manual equation-level source audit

- `1508.07961`: quantum-cuboid/geometricity baseline; no global glued-complex measure formula.
- `1605.07649`: explicit coarse-to-fine decomposition/path-integral-measure discussion; no explicit global pullback/Jacobian formula.
- `1701.02311`: Appendix B explicitly derives the pullback/Faddeev-Popov-type factor for **one 4D hypercuboid** and says that the general case can be inferred. The paper also states that the geometric reduction leaves one coarse and six fine integration variables and that a nontrivial determinant is required. It does not write the incidence-aware global measure for the specific glued two-hypercuboid / `2x2x2x4` complexes.
- `1804.00023`: contains explicit coarse/fine Jacobians and delta constraints, but for the distinct hyperfrustum curvature truncation. Importing that Jacobian into RC008 would change the realization/object and requires a separate preregistered derivation or bridge gate.

Therefore the terminal ITER065 classification is:

**`SCOPED_BLOCKED_NO_EXPLICIT_GLOBAL_MEASURE_AUTHORITY`**.

Durable result commit: `aac1b50dbf5f0c72ea8b76ec3c046b68259c7e8f`.

This is a source-authority blocker, not a negative physical result. `restricted_coarse_fine_amplitude_reproduced=false`; corrected RC008 crossing remains unauthorized.

## Exact next scientific strategy

Do not spend more compute on the current RC008 crossing route unless either:

1. a genuinely new same-realization source supplies the missing global measure object; or
2. a separate prospective derivation gate is justified without importing the hyperfrustum measure by analogy.

The next continuation should compare the exact upstream blockers and downstream unlock value of independent PHASE_1 fronts and select one highest-information gate outside this saturated RC008 route. Current natural comparison set is the Lorentzian EPRL refinement-source front, RC009 with a genuinely new source-defined measure/contour route, and causal-set/BH004B.

No active compute is authoritative.

## Readiness

Overall scientific programme: **50% (Δ0)**. Candidate theory: **0% / UNFORMED**. Bridge credit: **0**.
