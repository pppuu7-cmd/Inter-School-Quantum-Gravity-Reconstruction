# ISQGR deep-research ledger — 2026-09-14 — ITER066 / ITER067

## STATE_READ

Repository recovery, machine state, interface atlas, realization cards, latest preregistrations/results/commits and GitHub Actions were reconstructed before new science. The repository state, not chat memory, was used as authority.

Initial recovered programme locks:

- phase `PHASE_1_AMPLITUDE_REFINEMENT_BRIDGE_ACTIVE`;
- candidate theory `UNFORMED`;
- theory established `0%`;
- bridge credit `0`;
- overall roadmap readiness `50%`;
- RC008 terminal source block on a global glued-complex measure;
- ITER066 active on BH004 causal-set quantum amplitude/refinement source authority.

No queued or in-progress competing Actions were present before the ITER066 repair.

## ITER066 — provenance resolution

Run `34878725306` was not a scientific failure. The Rideout lane completed its frozen exact-PDF source audit and failed only in `actions/upload-artifact` because the raw source id `gr-qc/0212064` was used in the artifact label. The exact missing JSON was not reconstructed by inference.

Run 1 durable classification:

`ARTIFACT_PRESERVATION_FAILURE_AFTER_SOURCE_AUDIT`.

A provenance-only workflow repair changed only the artifact transport label to `gr-qc-0212064`; the original extractor input, source set, extractor, selectors, classifier, question and claim ceiling remained unchanged.

Authoritative retry `34884258757` completed all three source lanes and aggregate successfully and preserved four immutable artifacts.

## ITER066 — source physics

Manual equation-level audit established:

- explicit QSG quantum measure/decoherence-functional and transition-amplitude objects: present, scoped;
- explicit classical CSG cosmological-renormalisation map: present;
- BDG action/continuum object: present, scoped;
- source-defined **quantum** scale/refinement transport on `D`, `mu` or amplitudes: missing from the frozen stack.

The classical transform cannot be silently applied to quantum-measure objects. Doing so would be a `NEW_BRIDGE_ASSUMPTION`.

Adversarial critic tried to promote Rideout transition amplitudes, the classical CSG map, cylinder-event nesting, and the BDG action continuum relation to the missing bridge object. Each attempt failed at the exact domain/dynamics/measure level.

Terminal ITER066:

`SCOPED_BLOCKED_NO_SOURCE_FAITHFUL_AMPLITUDE_REFINEMENT_OBJECT`.

This is scoped to the frozen three-source stack and is not a causal-set no-go theorem.

## Next-gate scheduling after ITER066

Candidate routes were judged against blocker severity, downstream unlock, falsifiability, source availability and cost.

Low-IG routes:

- repeating BH004 search over the same frozen sources;
- RC008 corrected crossing without a new global physical-measure source;
- further numerical scans without a newly defined physical object.

Chosen successor:

`ITER067_RC002_FULL_CDT_TRANSFER_MATRIX_COMPOSITION_MEASURE_AUTHORITY`.

Reason: RC002 already possessed a concrete regulated transfer-matrix object, so the gate could directly test the H0 composition coordinate without fabricated dynamics or heavy computation. It also had a sharp risk: possible conflation of the full spatial-triangulation operator with the measured volume-reduced effective transfer matrix.

## ITER067 — prospective freeze

Preregistration committed before substantive source audit. Frozen primary sources:

- `hep-th/0105267v1`;
- `1205.3791v1`;
- `1302.2210v1`.

Mandatory lanes A–F: full state space, full matrix element, composition, positivity/Hamiltonian, full-vs-effective distinction, preferred-time/Wick dependence. Six adversarial null controls were frozen.

No numerical Actions were launched because no scientific predicate depended on computation.

## ITER067 — source result

The 2001 primary source supplies a complete regulated transfer-matrix package:

- Eq. (43): state sum with `1/C(T)` measure;
- Eq. (44): automorphism-weighted state inner product and resolution of identity;
- Eq. (45): full one-step matrix element as a sum over all interpolating sandwich triangulations;
- Eq. (46): propagation by `T^t`;
- Eq. (47): exact semigroup/gluing law with intermediate `C(g)` weighting.

The `C(g)` factor is not cosmetic: the source derives why it restores the correct symmetry factor under gluing.

The source also yields a critical limitation. In `d>2`, the established positivity/Hamiltonian package applies to `T_N^2`; link-reflection positivity of elementary `T_N` is not established in `d=3,4`, and elementary `T_N` may fail to be positive. The source Hamiltonian is therefore built from the two-step object, with possible zero modes quotiented.

The 2012/2013 sources sharply distinguish the full spatial-triangulation transfer matrix from the volume/scale-factor effective matrix. The latter is a reduced/averaged effective object whose accuracy is tested numerically; it is not source-authorized as operator equality to the full matrix.

The composition construction depends essentially on the source's distinguished discrete proper time. The Lorentzian amplitude and Euclideanized positivity analysis are related by a source-defined Wick rotation but have distinct roles.

## ITER067 — adversarial critic

Attacks on the positive result:

- semigroup as mere bookkeeping;
- elementary-`T` positivity overclaim;
- combinatorial measure vs physical continuum measure;
- finite-cutoff “full” terminology;
- full/effective conflation;
- Wick-rotation conflation;
- preferred-foliation dependence;
- automorphism factors treated as arbitrary normalization.

The attacks narrowed but did not invalidate the preregistered regulated source object.

Countermodel: an arbitrary preferred-time transfer-matrix statistical system may satisfy a semigroup law without any gravitational continuum limit or common-parent relation. Therefore composition alone does not derive H0.

Terminal ITER067:

`PASS_SCOPED / STRUCTURAL_MAP_ESTABLISHED_SCOPED`.

Bridge credit remains zero.

## NEW_STRUCTURAL_FACT

Two adjacent gates now expose a sharper taxonomy than the generic word “composition/refinement”:

1. causal-set frozen stack: quantum history measure exists, classical scale flow exists, but their quantum scale/refinement transport is missing;
2. CDT: regulated sequential temporal composition is exact and measure-coupled, but it relies on preferred discrete time and only has the source-qualified positivity package for `T^2` in higher dimensions;
3. therefore a common-parent candidate cannot merely say “both theories compose”. It must type the operation and derive/translate state space, measure, quotient and causal/time semantics.

## WHAT_WAS_RULED_OUT

- red ITER066 CI = scientific fail;
- post-hoc reconstruction of a missing Rideout artifact;
- classical CSG renormalisation = QSG refinement;
- BDG continuum action = quantum history refinement;
- CDT volume-effective matrix = full CDT transfer operator;
- elementary 4D CDT `T` positivity as an established theorem;
- automorphism factors as disposable normalization;
- preferred-time CDT semigroup as a representation-independent common-parent principle.

## NEXT ADMISSIBLE GATE

`PREREGISTER_ITER068_CDT_HAPPY_COMPOSITION_TYPE_DISCRIMINATION`

Question to freeze prospectively: can source-qualified CDT sequential proper-time composition and exact HaPPY tensor/QEC subsystem composition be represented as instances of a single physically meaningful composition principle **without** erasing their different domains/codomains, dynamics, normalization/isometry structure, causal semantics and observables? Or does the proposed common `composition` coordinate split into inequivalent operation types?

Why this is the highest-IG next gate:

- both sides have explicit exact composition objects and normalization structure;
- it directly attacks a central common-parent ambiguity rather than extending a blocked numerical lane;
- it is cheap and theorem/counterexample-oriented;
- a negative result would eliminate a broad class of false correspondences that collapse temporal succession and tensor/subsystem contraction into one symbol;
- a positive result would still require explicit typed arrows and would only be mechanism-level because HaPPY is a toy model, preventing overclaim;
- Lorentzian EPRL composition remains source-blocked in RC001 and RC008 remains globally measure-blocked, making them poorer immediate comparators.

No ITER068 source conclusion is authorized until its own preregistration is committed.

## Locks after this run

`CANDIDATE_THEORY=UNFORMED`

`THEORY_ESTABLISHED=0%`

`BRIDGE_CREDIT=0`

Overall roadmap readiness remains `50%`; the new ITER067 PASS is a scoped source-qualified component, not a probability increase or full bridge derivation.
