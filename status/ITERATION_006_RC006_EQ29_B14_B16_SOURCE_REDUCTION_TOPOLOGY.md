# SUPERSEDED BEFORE IMPLEMENTATION — Iteration 006 / RC006 Eq.(29) ↔ B14 direct-topology proposal

This preregistration was **withdrawn before any implementation or production result existed**. It must never be used as a scientific gate or result.

Reason: a consistency review against already validated, pre-existing Eq.(29) source-incidence evidence (`SOURCE_PATH_INCIDENCE_QUALIFIED`, run `34718554940`) shows that the immutable Eq.(29) drawing contains 18 geometric primitives and, after suppressing six degree-2 bend vertices, has an 8-vertex / 12-edge trivalent logical graph. Its source label list has ten distinct spin variables. Therefore a proposed direct isomorphism to the Appendix-B B14 nine-edge graph was not the correct scientific object. Launching that comparison would have been knowingly low-information and inconsistent with already available source evidence.

No workflow was implemented and no production data were viewed under this preregistration. No threshold or model was changed in response to a production result.

The admissible replacement object is the **source derivation chain actually stated by the paper**: Appendix F says the 3-valent EPRL diagrams are obtained by applying the B15 identities to the 4-valent construction and then including the R-matrix identity. A separate prospective gate must qualify that literal Eq.(29) / Appendix-F / B15 dependency chain before any numerical amplitude reconstruction.

Claim locks remain unchanged: `eq29_amplitude_authorized=false`, `bridge_credit=false`, candidate theory `0/UNFORMED`.
