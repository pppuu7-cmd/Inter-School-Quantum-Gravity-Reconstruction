# ITER126 adversarial critic — line endpoint/coincidence pole extraction

Date: 2026-09-15
Gate: `ITER126_FIXED_GEODESIC_CURVATURE_LINE_ENDPOINT_COINCIDENCE_POLE_EXTRACTION_AUTHORITY`
Source authority: `sources/ITER126_FIXED_GEODESIC_CURVATURE_LINE_ENDPOINT_COINCIDENCE_POLE_EXTRACTION_AUTHORITY_2026-09-15.md`

## Attack 1 — power divergences vanish in dimensional regularization, so the endpoint prototypes should be set to zero

Rejected. A blanket "power divergences vanish" rule applies to genuinely scaleless momentum integrals, not to analytically continued defect-parameter distributions with a physical line length and nontrivial smooth weights. The pole of `x^{-m+a epsilon}` is distributional and can be controlled by a derivative/Taylor coefficient of the test weight.

ITER126 explicitly demonstrates this: for `m=2` the logarithmic `1/epsilon` residue is controlled by the linear Taylor coefficient `w_1`, not by the constant term.

## Attack 2 — the endpoint-product residue +2 is universal B1 data

Rejected. Derivative/tensor numerators and source-specific line weights change both the singular power and local Taylor coefficients. `+2` only validates the subtraction machinery for the scalar prototype.

## Attack 3 — the diagonal prototype -1 is a scaleless self-contraction artifact

Rejected. For `tau != sigma` the line separation `l|tau-sigma|` supplies a physical coordinate scale. The pole arises from the local coincidence region after integrating along the finite defect. It is not the same object as a momentum tadpole with no external or coordinate scale.

## Attack 4 — finite interior line regions can secretly affect the UV pole through oscillatory phases

After bulk subdivergences are removed, a compact region bounded away from all coordinate coincidences has smooth propagators/derivatives and a finite `epsilon -> 0` limit. Oscillatory phases there can alter finite parts but not generate a UV pole. Their only influence on a local pole is through the Taylor coefficients with which they approach a singular stratum.

## Attack 5 — massless infrared poles could be confused with UV poles

This is a valid implementation warning but not a failure of the local defect analysis. ITER124 freezes nonzero external `Q` and nonzero physical geodesic length. The ordinary massless two-point bubble is IR safe for generic nonzero `Q` in four dimensions, and the line-separated one-propagator transform is IR integrable. If an individual rearranged master introduces an artificial IR pole, an auxiliary IR regulator or region diagnostic must separate it before assigning UV counterterms.

## Attack 6 — one-dimensional Taylor subtraction is enough for all M2 integrals

Rejected. Overlapping endpoint/diagonal regions require sector decomposition or an equivalent forest construction. ITER126 explicitly does not authorize independent one-dimensional subtractions that double-count intersections.

## Attack 7 — only the leading Taylor coefficient of a smooth weight matters

False for power-singular defect kernels. The general formula shows that the pole of `x^{-m+a epsilon}` is controlled by the coefficient of `x^(m-1)`. For derivatives that increase `m`, correspondingly higher local Taylor coefficients can be required. The algorithm freezes the finite expansion order by power counting rather than always taking `w(0)`.

## Attack 8 — contact derivatives generated by curvature can be ignored in the asymptotic expansion

Rejected during renormalization. Derivatives can generate endpoint/contact distributions needed for subdivergence cancellation. They may be removed from the final separated coefficient only after the ITER124 subtraction order is completed.

## Critic verdict

**CONFIRMS `PASS_SCOPED_DEFECT_POLES_LOCAL_ENDPOINT_COINCIDENCE_ASYMPTOTIC_SUBTRACTION_AUTHORIZED`.**

ITER126 authorizes local pole extraction, not transfer of prototype residues to curvature. The next real calculation is to instantiate the local powers/weights for the actual projected `chi_1/chi_2` M/G kernels.

Bridge credit: **0**. Candidate theory: **UNFORMED / 0%**.