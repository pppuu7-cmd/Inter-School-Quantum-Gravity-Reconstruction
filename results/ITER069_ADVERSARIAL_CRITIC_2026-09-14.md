# ITER069 adversarial critic — RC003 FRG scale/composition authority

Date: 2026-09-14
Gate: `ITER069_RC003_FRG_SCALE_COMPOSITION_AUTHORITY`
Source-authority commit: `2a851fab018fbf72d49c895b14a41d68d2804624`

## Target

Attempt to invalidate or strengthen the researcher classification `PASS_SCOPED_FRG_SCALE_OPERATION_QUALIFIED` by searching for hidden equivalences between FRG scale flow and the already typed ISQGR composition objects.

## Attack 1 — promote autonomous RG flow to a semigroup

A sufficiently regular autonomous beta-function system can induce flow maps satisfying a mathematical composition property. This does not satisfy the frozen scientific requirement by itself. The source-qualified object is the EAA/FRG differential flow with regulator, gauge/background and truncation structure; the frozen stack does not present an exact finite `U(k2,k1)` as a physical coarse-graining operator with all required typing.

Verdict: `REJECT_STRENGTHENING`. Mathematical flow composition is weaker than the requested source-defined physical composition law.

## Attack 2 — identify RG time with CDT proper time

The notation `t = ln(k/k0)` might superficially resemble an evolution parameter. In the frozen stack it is an RG parameter associated with momentum/coarse-graining scale. The 2026 review explicitly distinguishes cutoff scale from physical scale. CDT's qualified object instead composes amplitudes between spatial triangulation states across preferred discrete proper-time slices.

Verdict: `TYPE_ERROR`. `C_scale_flow != C_seq` without a new bridge map.

## Attack 3 — identify shell integration with HaPPY tensor contraction

Both operations may be described informally as eliminating/internalizing degrees of freedom. FRG changes a scale-dependent effective action through regulated functional integration. HaPPY contracts tensor indices to define an isometric bulk-to-boundary encoding with QEC semantics. Regulator dependence, domain/codomain, normalization and observable meaning do not match under any source-defined map in the frozen evidence.

Verdict: `TYPE_ERROR`. `C_scale_flow != C_tensor` under the available authority.

## Attack 4 — use fixed-point universality as a common parent

Fixed points and critical exponents can exhibit universality under changes of microscopic description or scheme. This may eventually support a cross-school bridge, but a fixed point is not itself a typed map between the physical composition structures already audited. It does not derive CDT temporal gluing, HaPPY encoding, causal-set quantum refinement or EPRL refinement.

Verdict: `INSUFFICIENT_FOR_BRIDGE`.

## Attack 5 — erase regulator/background/truncation qualifiers

Dropping `R_k`, background/gauge structure or truncation status produces a generic statement such as 'coarse graining generates a flow'. That statement is too weak for ISQGR bridge accounting and violates the preregistered controls.

Verdict: `FORGETFUL_COMMONALITY_ONLY`.

## Attack 6 — demote the result to source-blocked

Predicates A-D and F are established by the frozen source stack. The preregistration explicitly allows predicate E to close as a scoped absence if adjudicated exactly. Therefore lack of a finite source-defined composition law does not force `BLOCKED_SOURCE_AUTHORITY`.

Verdict: `REJECT_DEMOTION`.

## Critic verdict

**CONFIRMS `PASS_SCOPED_FRG_SCALE_OPERATION_QUALIFIED`.**

Mandatory retained qualifier:

**`FINITE_SOURCE_DEFINED_COMPOSITION_LAW = NOT_ESTABLISHED_IN_FROZEN_STACK`.**

The strongest defensible structural conclusion is that ISQGR now has a third composition/coarse-graining type:

- `C_seq`: sequential temporal/state-sum composition (CDT);
- `C_tensor`: subsystem/tensor contraction/isometric encoding (HaPPY);
- `C_scale_flow`: regulator-dependent theory-space/EAA coarse-graining flow (FRG).

The three share increasingly abstract algebraic language, but no source-qualified physical equivalence between them has been derived.

## Claim ceiling

No bridge credit. No candidate theory. No universal parent. No new physics claim. No authorization to relabel RG scale as physical time or refinement.