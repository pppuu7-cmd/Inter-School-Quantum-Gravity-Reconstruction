# ITER068 terminal result — CDT / HaPPY composition-type discrimination

Date: 2026-09-14
Gate: `ITER068_CDT_HAPPY_COMPOSITION_TYPE_DISCRIMINATION`
Preregistration commit: `a50779bcd93ba34546b16751d61245ad45e6ee01`
Source/structure audit commit: `af299c1d125799595dbe77a0a2c49fffed4f1404`
Adversarial critic commit: `24e232d9f26cb9a5ee81a06c16c0131bd52c4234`

## Terminal scientific classification

**`FAIL_SCOPED / PHYSICAL_COMPOSITION_TYPE_EQUIVALENCE_REJECTED`**

Retained positive sub-result:

**`COMMON_ALGEBRAIC_KERNEL_ONLY`**

## Question adjudicated

Can the source-qualified CDT regulated full-state temporal composition package and the exact HaPPY perfect-tensor/QEC composition package be embedded into one physically meaningful typed composition principle while preserving or deriving:

- domain/codomain;
- composition law;
- normalization/measure;
- dynamics;
- causal/time semantics;
- observable meaning;
- quotient/equivalence structure?

## Researcher result

No.

A valid common algebraic host exists: both mechanisms can be represented by linear maps/tensors and composition can be expressed as contraction over intermediate indices. This weak commonization is exact but **forgetful**.

The physical objects differ in mandatory type:

### CDT

`C_seq`: temporal/state-sum sequential composition.

Source-qualified structure includes:

- spatial triangulation states at discrete proper-time slices;
- action-weighted sums over interpolating sandwich histories;
- automorphism measure `1/C(T)`;
- intermediate-state factor `C(g)`;
- exact semigroup/gluing law;
- source-defined discrete proper-time semantics;
- regulated transition-amplitude interpretation.

### HaPPY

`C_tensor`: subsystem/tensor-contraction composition implementing isometric encoding.

Source-qualified structure includes:

- perfect tensors;
- contracted internal Hilbert-space indices;
- bulk logical inputs and boundary physical outputs;
- global bulk-to-boundary isometry;
- inner-product preservation / QEC encoding;
- logical-operator reconstruction and entanglement observables;
- no source-defined identification of tensor-network depth with Lorentzian proper-time evolution in this frozen toy realization.

## Why PASS fails

The following identifications are not derived by the frozen sources:

1. `CDT proper time <-> HaPPY layer/radial depth`;
2. `CDT transfer dynamics <-> HaPPY QECC encoding`;
3. `CDT automorphism state-sum measure <-> HaPPY isometric normalization`;
4. `CDT spatial-geometry transition observables <-> HaPPY logical/QEC observables`;
5. `CDT triangulation automorphism quotient <-> HaPPY tensor/code equivalence`.

Each would require a `NEW_BRIDGE_ASSUMPTION`.

Therefore the two objects share an algebraic representation, but the source stack does not derive their equivalence as a physical composition type.

## Controls

All preregistered controls behaved as required:

- `FORGETFUL_LINEARIZATION_CONTROL`: detects exactly the weak algebraic commonality;
- `TIME_RADIAL_SWAP_CONTROL`: rejected;
- `NORMALIZATION_DROP_CONTROL`: detects structure loss;
- `FULL_EFFECTIVE_COLLAPSE_CONTROL`: rejected by ITER067 authority;
- `DYNAMICS_ENCODING_SWAP_CONTROL`: rejected;
- `OBSERVABLE_ERASURE_CONTROL`: detects loss of physical meaning;
- `TOY_MODEL_PROMOTION_CONTROL`: rejected.

## Adversarial critic result

The critic attempted stronger common parents via:

- the category of Hilbert spaces and linear maps;
- an isometry-only common category;
- layer-depth/proper-time identification;
- common normalization language;
- operator-pushing / Heisenberg-style transport;
- dagger-category representation;
- reclassification as source-blocked;
- promotion from HaPPY toy code to generic holographic gravity.

None satisfies the frozen strong PASS conditions without either forgetting mandatory physical structure or importing new assumptions.

Critic verdict: **CONFIRMS researcher FAIL**.

## New structural fact

The Interface-Atlas coordinate `C = composition/gluing` is too coarse for bridge accounting.

At minimum it must now distinguish:

- `C_seq` — sequential/temporal state-sum composition;
- `C_tensor` — subsystem/tensor-contraction/isometric-encoding composition.

The generic sentence "both theories compose" is therefore falsified as bridge evidence for this pair.

A future parent principle is not ruled out, but it must derive explicit typed embeddings preserving the physical semantics instead of identifying the two by generic associativity/contraction.

## What was ruled out

Within the frozen ITER068 scope:

- direct physical equivalence of the CDT and HaPPY composition mechanisms;
- use of generic matrix/tensor multiplication as sufficient bridge evidence;
- proper-time/network-depth identification without new physics/assumption;
- measure/isometry identification by terminology alone;
- dynamics/encoding identification;
- observable erasure as a route to commonality.

## What remains open

- a more general parent category enriched with additional physical structure may still exist;
- a dynamical holographic realization could have a different sequential-composition object than static HaPPY;
- `C_tensor` may recur source-faithfully in tensor/GFT or spin-net coarse-graining realizations;
- `C_seq` may recur in another genuine path-integral/state-sum realization;
- H0 may survive only in a typed, multi-operation form rather than a single undifferentiated `composition/refinement` law.

## Downstream authorization

No numerical workload is authorized by this result.

The next highest-information question is orthogonal: whether continuum FRG/EAA scale evolution supplies a source-defined composition/coarse-graining algebra that can be related to either `C_seq`, `C_tensor`, or a distinct typed operation without analogy.

Recommended next gate:

**`PREREGISTER_ITER069_RC003_FRG_SCALE_COMPOSITION_AUTHORITY`**

This gate should freeze the exact Wetterich/EAA coarse-graining object, regulator dependence, domain/codomain in theory space, gauge/background structure, and the mathematical relation (if any) between RG scale evolution and physical composition/refinement. It should first ask whether the needed common physical object exists at all before any cross-school numerical comparison.

## Claim ceiling

This result does **not** establish:

- failure of CDT;
- failure of HaPPY/holography;
- failure of all composition-based QG bridges;
- a universal no-go theorem;
- `BRIDGE_DERIVED`;
- `UNIVERSAL_COMMON_PARENT_FOUND`;
- new physics;
- a new QG theory.

`CANDIDATE_THEORY = UNFORMED`

`THEORY_ESTABLISHED = 0%`

`BRIDGE_CREDIT = 0`
