# ITER088 terminal result — retrospective CdS spatial-gap infrared scaling adjudication

Date: 2026-09-15
Gate: `ITER088_RETROSPECTIVE_CDS_SPATIAL_GAP_IR_SCALING_ADJUDICATION`
Protocol: `385f70a3c24f3f4dc39a5ba8e2380516786ad5d7`
Source authority: `152d258b9919f1958beb80206d776363e7d4a9cd`
Adversarial review: `edae25dd7fada8b0caab9dce201b5901dc7a1064`

## Terminal classification

**`SOURCE_REJECTS_ROUND_S3_IR_SCALING_ACCESSIBLE_CDS`**

Prospective validation credit: **0** by protocol.
Bridge credit: **0**.
Candidate theory: **UNFORMED / 0%**.

## Result

The published 2019 `C_dS` spatial-spectrum analysis directly tests the finite-size form

`<lambda_n> = A_n V_S^(-2/d_eff)`

at fixed bare couplings. For low modes `n=1,3,5` it reports good power-law fits with

**`d_eff approximately 1.6`**,

and states that similar results hold for modes up to a few tens.

The round-three-sphere residue of ITER087 would require

`d_eff=3`,

or exponent `2/3`. The source instead gives exponent approximately `2/1.6 = 1.25`.

A multiplicative normalization cannot repair an exponent mismatch. Therefore the accessible finite-regulator `C_dS` low-mode spectrum rejects the naive round-`S^3` infrared scaling law.

## Positive retained result

The spectral gap still closes as `V_S -> infinity` in `C_dS`. Thus the source supports an extended finite-dimensional spatial geometry, but the measured large-scale spectral effective dimension is approximately 1.6 rather than 3 in the studied regime.

## Structural consequence

This closes the attempted route from the global de-Sitter/minisuperspace radius to the first nonconstant spatial Laplacian mode.

The global volume sector and the nonconstant spatial spectral sector are empirically distinct enough that a de-Sitter-like mean `N_3(t)` profile cannot be promoted to a round-slice spectral identity.

This also clarifies the ITER086 failure: the problem is not only the choice of cutoff threshold. The canonical low spatial modes themselves do not realize the round-slice spectrum assumed by that bridge hypothesis in the accessible ensembles.

## Continuum ceiling

The result is finite-regulator and finite-size source authority. It does not establish that the ultimate continuum infrared spatial dimension is 1.6. A different scaling regime could emerge beyond accessible volumes or along a continuum trajectory.

## Next scientific direction

Do not reopen the spatial-gap/minisuperspace-radius route by changing mode or normalization.

The highest-information successor should remain in the **global/constant-mode sector** of the reduced CDT action, where the direct 2024 source itself states that global volume fluctuations are carried by constant modes. Search for a second independent global observable in the CDT volume-fluctuation covariance/Hessian that can be typed against the FRG minisuperspace Hessian or curvature of the reduced effective action.

This successor must be source-authority first: establish whether the eigenmodes/eigenvalues of the CDT volume covariance matrix have a direct continuum minisuperspace counterpart with source-defined normalization before attempting any FRG coupling inference.
