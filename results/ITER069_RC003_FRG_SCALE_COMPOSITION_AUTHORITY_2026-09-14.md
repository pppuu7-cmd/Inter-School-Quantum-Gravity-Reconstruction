# ITER069 terminal result — RC003 FRG scale/composition authority

Date: 2026-09-14
Gate: `ITER069_RC003_FRG_SCALE_COMPOSITION_AUTHORITY`
Preregistration commit: `c938f74feed0e1aae729cdafea05a3d6e5d70227`
Extraction/CI commit: `89aab44905ebc8406bf65711c384a39576c9ad5b`
Authoritative CI run: `34889707865`
Source-authority audit commit: `2a851fab018fbf72d49c895b14a41d68d2804624`
Adversarial critic commit: `8639ccacef0f54c1bd5cc773ca19a3560d088a98`

## Terminal scientific classification

**`PASS_SCOPED / FRG_SCALE_OPERATION_QUALIFIED`**

Mandatory retained qualifier:

**`FINITE_SOURCE_DEFINED_COMPOSITION_LAW = NOT_ESTABLISHED_IN_FROZEN_STACK`**

Bridge credit: **0**.

## Question adjudicated

Does the frozen EAA/FRG source stack define a sufficiently typed coarse-graining/composition object to be compared with the composition/refinement mechanisms already isolated in ISQGR, without identifying RG scale evolution with temporal gluing, tensor contraction or geometric refinement by analogy?

## Result

Yes, but only at the level of a distinct, source-qualified scale-flow operation.

The frozen sources qualify the effective average action `Gamma_k`, the Wetterich functional flow, regulator insertion `R_k`, background/gauge/ghost structure, theory-space trajectories and fixed-point language. They also support the crucial typing statement that the running object is a scale-indexed effective action / set of couplings in theory space, not a source-defined physical spacetime-state evolution.

The qualified operation is therefore introduced as:

**`C_scale_flow` — regulator-dependent continuum coarse-graining / theory-space flow of effective actions.**

This operation is scientifically real within the frozen source scope, but it is not yet a physical composition object common to the other realization classes.

## Preregistered predicate results

- A — exact EAA/Wetterich object: **PASS**.
- B — domain/codomain typed as effective actions/functionals/couplings at scale `k`: **PASS**.
- C — regulator insertion/dependence explicit: **PASS**.
- D — gravity gauge/background/ghost structure explicit: **PASS**.
- E — exact source-defined finite `k1 -> k2` composition/semigroup law: **SCOPED ABSENCE** in the frozen stack.
- F — fixed-point/trajectory language separated from physical state/geometric refinement: **PASS**.
- G — no cross-school identification by analogy: **PASS NEGATIVE LOCK**.

The preregistration explicitly permits a scoped PASS when A-D and F are source-qualified and E is exactly adjudicated, including a documented absence. That condition is met.

## Three-way composition-type split

ITER068 already established that the atlas coordinate `C = composition/gluing` was too coarse for CDT and HaPPY. ITER069 sharpens the split further:

### `C_seq` — CDT sequential temporal/state-sum composition

- domain/codomain: spatial triangulation states on proper-time slices;
- composition: exact regulated gluing/semigroup over an intermediate spatial state;
- measure: action and automorphism weighted state sum;
- semantics: causal/temporal transition amplitude in the sliced CDT construction.

### `C_tensor` — HaPPY subsystem/tensor-contraction composition

- domain/codomain: tensor-product Hilbert-space factors / logical-to-physical code spaces;
- composition: contraction of internal tensor indices;
- normalization: isometric/perfect-tensor structure;
- semantics: QEC encoding, reconstruction and entanglement structure, not proper-time dynamics.

### `C_scale_flow` — FRG/EAA scale flow

- domain/codomain: scale-indexed effective actions/functionals/couplings in theory space;
- local law: differential functional RG/Wetterich flow;
- regulator: explicit `R_k` / coarse-graining kernel is part of the qualified object;
- gravitational representation: background-field, gauge-fixing and ghost structure is non-erasable;
- semantics: continuum scale evolution/coarse graining in theory space, not source-defined spacetime-state evolution.

The three can all be represented in increasingly abstract mathematical languages involving maps, contractions, trajectories or composition. That commonality is too forgetful to count as a physical bridge.

## Why no stronger bridge follows

The frozen evidence does not derive any of the following typed identifications:

1. FRG `k` or RG time `<->` CDT proper time;
2. FRG integration/coarse graining `<->` HaPPY tensor contraction/isometric encoding;
3. FRG fixed point `<->` physical refinement fixed object in CDT/EPRL/causal sets;
4. regulator/scheme structure `<->` CDT automorphism measure or HaPPY normalization;
5. theory-space effective-action observables `<->` geometry-transition or QEC observables.

Each stronger statement requires a new bridge map with preserved or derived semantics.

## Adversarial critic

The critic attempted to strengthen the result using autonomous-flow composition, RG-time language, generic coarse-graining, fixed-point universality and forgetful linear/categorical common hosts. None supplies the missing typed physical equivalence.

The critic also attempted to demote the gate to source-blocked. That fails because the frozen stack establishes A-D and F, while the preregistration allows E to close as a scoped absence.

Critic verdict: **CONFIRMS researcher PASS_SCOPED**.

## Structural update to H0

A hypothesis in which `composition/refinement` is a single undifferentiated primitive is no longer compatible with the accumulated source-qualified evidence.

The surviving form of H0, if any, must be **typed and multi-operation**. A deeper common parent would have to provide explicit embeddings or functors that preserve/derive at minimum:

- domain and codomain;
- measure/normalization/regulator structure;
- dynamics;
- causal/time semantics;
- observables;
- quotient/gauge structure;
- scale/refinement meaning.

Generic associativity, shell integration, tensor contraction or the word `coarse graining` is insufficient.

## Downstream authorization

No numerical workload is authorized by ITER069.

The highest-information next question is whether `C_scale_flow` and the strongest already-qualified physical dynamical composition object, CDT `C_seq`, share any non-forgetful typed invariant once physical scale, regulator, measure, state space, dynamics and observables are retained.

Recommended next gate:

**`PREREGISTER_ITER070_FRG_CDT_SCALE_TIME_TYPE_DISCRIMINATION`**

This gate should prospectively freeze candidate invariants and explicit failure controls before comparing the two mechanisms. In particular it must forbid `RG time = proper time`, regulator erasure, reduced/full-state substitution and fixed-point/refinement substitution.

## Claim ceiling

ITER069 does **not** establish:

- `BRIDGE_DERIVED`;
- `UNIVERSAL_COMMON_PARENT_FOUND`;
- physical equivalence of FRG and CDT/HaPPY/EPRL/causal-set operations;
- a complete quantum-gravity theory;
- new physics;
- a candidate theory.

`CANDIDATE_THEORY = UNFORMED`

`THEORY_ESTABLISHED = 0%`

`BRIDGE_CREDIT = 0`
