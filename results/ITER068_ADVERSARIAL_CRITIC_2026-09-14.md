# ITER068 adversarial critic — CDT / HaPPY composition-type discrimination

Date: 2026-09-14
Gate: `ITER068_CDT_HAPPY_COMPOSITION_TYPE_DISCRIMINATION`
Frozen prereg commit: `a50779bcd93ba34546b16751d61245ad45e6ee01`
Researcher source audit: `sources/ITER068_CDT_HAPPY_COMPOSITION_TYPE_AUDIT_2026-09-14.md`

## Critic mandate

Attempt to destroy the researcher conclusion

`FAIL_SCOPED / PHYSICAL_COMPOSITION_TYPE_EQUIVALENCE_REJECTED`

by constructing, from the frozen sources alone, a stronger common typed composition map that preserves the preregistered mandatory coordinates.

The critic is not allowed to weaken PASS criteria or introduce post-result bridge assumptions.

## Attack 1 — use `Hilb` / linear maps as the common parent

Both mechanisms can be represented as linear maps and their composition is associative. CDT transfer amplitudes form matrix elements of a transfer operator; HaPPY local tensors/network define linear maps and, for the code, isometries.

Candidate parent:

`P = finite-dimensional Hilbert spaces with linear maps`.

### Critic finding

This does construct a mathematically valid common category, but it fails the preregistered *physical* condition. The embedding forgets:

- CDT proper-time semantics;
- CDT action/history meaning;
- CDT automorphism measure factors as physical/combinatorial gluing data;
- HaPPY logical-to-physical encoding semantics;
- HaPPY isometric/QEC constraint;
- native observable meanings.

Therefore it witnesses `COMMON_ALGEBRAIC_KERNEL_ONLY`, exactly the preregistered null control, not a strong PASS.

**Attack fails.**

## Attack 2 — restrict the parent to isometries

HaPPY lies naturally in an isometric subcategory. Perhaps CDT transfer evolution can also be treated as isometric after suitable normalization.

### Critic finding

The frozen CDT authority does not establish an isometric one-step transfer map. Its relevant positivity statement in d>2 is only for `T_N^2`, and the Hamiltonian construction proceeds through the positive two-step object after Wick rotation/quotient, not through a source-defined QEC-style isometric encoder.

Any polar decomposition, Stinespring dilation, rescaling, ancillary extension or ad hoc normalized embedding would be new mathematical structure not supplied by the frozen CDT source.

**Attack fails; proposed repair = `NEW_BRIDGE_ASSUMPTION`.**

## Attack 3 — identify HaPPY network depth with CDT proper time

The HaPPY proof orders tensors in layers by graph distance from the center. CDT also composes one discrete layer at a time. Could layer number be the common sequential variable?

### Critic finding

In HaPPY the layer ordering is used to prove that the global tensor network is an isometry. The source describes the network as a hyperbolic bulk/boundary code and explicitly emphasizes that the infinite uniform tiling has no inherent directionality. The frozen source does not identify radial graph distance with Lorentzian proper time, causal succession, or a gravitational history variable.

CDT, by contrast, defines composition by consecutive proper-time sandwiches; the time label is structural to the source-qualified regulated geometry.

The proposed identification changes semantic type and therefore cannot be derived from the sources.

**Attack fails; `TIME_RADIAL_SWAP_CONTROL` survives.**

## Attack 4 — identify both normalization structures abstractly

Maybe CDT automorphism weights and HaPPY isometry are both manifestations of an abstract inner-product-compatible composition principle.

### Critic finding

At a sufficiently abstract level both structures constrain composition, but their source-defined roles are different:

- CDT `1/C(T)` counts inequivalent histories with automorphism weight and `C(g)` enters the intermediate-state resolution/gluing law;
- HaPPY `V^dagger V = I` states that the encoder preserves inner products on the input code space.

No frozen source supplies a functor or quotient identifying automorphism weighting with isometric normalization. Calling both `normalization` is taxonomy, not derivation.

**Attack fails.**

## Attack 5 — use operator composition / Heisenberg transport as observable bridge

HaPPY has operator pushing `O -> V O V^dagger` on the code image. CDT has a transfer operator, so one might define `O -> T^dagger O T` and identify the two.

### Critic finding

The HaPPY relation is source-defined from isometry and underlies logical operator reconstruction. The analogous CDT expression would be an externally chosen operator transform; the frozen CDT gate does not identify this with its physical observables or with QEC reconstruction, and the one-step transfer map is not source-qualified as an isometry/unitary.

Thus the map imports both an observable prescription and a physical interpretation.

**Attack fails; `OBSERVABLE_ERASURE_CONTROL` cannot be bypassed.**

## Attack 6 — regard both as path/tensor contractions in a dagger category

A more sophisticated categorical parent might retain inner products, daggers and contractions while allowing non-isometric CDT morphisms and isometric HaPPY morphisms.

### Critic finding

This is a legitimate **mathematical host category**, but it does not derive the physical substructures. It can *represent* both maps, not explain why CDT's history measure and temporal composition or HaPPY's QEC encoding arise from one physical parent law.

Classification hierarchy:

- `CAN_REPRESENT`: yes;
- `CAN_EMBED` algebraically: yes;
- `COMPATIBLE_WITH` a broad linear/categorical framework: yes;
- `DERIVES` the physical structures: no;
- `EQUIVALENT_TO`: no.

This distinction is decisive under the frozen gate.

**Attack fails.**

## Attack 7 — argue the gate is BLOCKED rather than FAIL

Perhaps absence of an explicit cross-school map in the sources means source authority is insufficient, so the correct result should be `BLOCKED_SOURCE_AUTHORITY`.

### Critic finding

The gate did not ask whether the literature already states a CDT↔HaPPY bridge. It prospectively asked whether explicit typed maps can be constructed from the *qualified source objects* while preserving mandatory coordinates. Both local mechanisms are adequately defined for that discrimination.

The strong hypothesis fails because its required identifications demand new assumptions, not because either compared object is undefined.

Therefore `BLOCKED` would be too weak for the preregistered hypothesis. The exact negative result is that the two frozen mechanisms do not instantiate the requested source-faithful **physical equivalence of composition type** under the permitted maps.

**Attack fails; FAIL remains appropriate.**

## Attack 8 — toy-model scope leakage

Could the HaPPY mechanism be promoted to generic AdS/CFT or holographic gravity to supply missing dynamics/time semantics?

### Critic finding

That move violates the frozen realization scope. RC005 and the primary paper qualify an exactly solvable toy code mechanism, not a complete dynamical gravity theory. Bringing in generic AdS/CFT dynamics after observing the mismatch would change the realization and the gate.

A future gate may compare CDT to a genuinely dynamical holographic realization, but ITER068 cannot do so retroactively.

**Attack fails; scope firewall intact.**

## Critic verdict

No source-faithful strong common typed map survives all mandatory coordinates.

The strongest surviving common structure is:

`COMMON_ALGEBRAIC_KERNEL_ONLY`

consisting of associative linear-map/tensor contraction after forgetting the physical typing that makes the two mechanisms different.

The critic therefore **CONFIRMS**:

`FAIL_SCOPED / PHYSICAL_COMPOSITION_TYPE_EQUIVALENCE_REJECTED`.

## Scientific consequence

The atlas coordinate `C` must not be counted as cross-school bridge evidence without a composition subtype. For current records at minimum:

- `C_seq`: sequential/temporal state-sum composition;
- `C_tensor`: tensor/subsystem/isometric-encoding composition.

This is a falsification of one broad false-correspondence class, not a no-go theorem against all common-parent composition principles.

## Claim ceiling

No bridge credit, universal no-go, full-QG statement, candidate theory, or new physics follows.