# ITER120 terminal result — fixed-geodesic curvature field-redefinition / defect quotient

Date: 2026-09-15
Gate: `ITER120_FIXED_GEODESIC_CURVATURE_FIELD_REDEFINITION_DEFECT_QUOTIENT`
Preregistration: `8759d566ad880178f1b59ff5996ac4489873f9b8`
Source authority: `9700bd579f9efb4bf2071ff35cb2d9de28759ee6`
Adversarial review: `f6b9f206f099f188c34f8cda5a1c0db39bc22aad`

## Terminal classification

**`PASS_SCOPED_DIM2_LINE_SECTORS_FIELD_REDEFINITION_REDUNDANT_GENUINE_DEFECT_RANK_TWO`**

Bridge credit: **0**.
Candidate theory: **UNFORMED / 0%**.

## Main result

The four line-interior curvature sectors from ITER118 split into:

### Field-redefinition redundant subspace

`span{int ds R, int ds R_nn}`.

For the standard one-loop local metric redefinition

`delta g_mn = kappa^2[a R_mn+b g_mn R]`,

the geodesic length varies as

`delta l = (kappa^2/2)[a int ds R_nn+b int ds R]`.

Thus these two line directions are exactly induced by the two independent local curvature-linear metric field-coordinate changes.

They are representation/scheme directions only when the action, geodesic and endpoint curvature observable are transformed consistently. Dropping their compensating endpoint/action shifts would change the observable and is not authorized.

### Genuine defect quotient at the frozen order

`span{int ds Box_perp R, int ds Box_perp R_nn}`.

These dimension-4 line densities cannot be generated by an `O(kappa^2)` local metric field redefinition without introducing another scale or moving to higher EFT order. They remain genuine observable-specific defect directions in the frozen calculation.

Therefore

**`dim(V_line / V_field-redefinition)=2`.**

## Consequence for the pole/log calculation

The projected running contribution to `B_1` need not treat all four line operators as independent physical matching directions. The first pole calculation can focus on the two genuine transverse-derivative line operators, while the redundant `int R/int R_nn` pair is retained as a field-redefinition invariance/checking sector.

This reduces the genuinely new curvature-specific line-defect problem from four directions to two.

## Exact successor

`ITER121_FIXED_GEODESIC_CURVATURE_GENUINE_DEFECT_B1_PROJECTION_AUTHORITY`

Compute or analytically reduce the pole/running projection of

`int ds Box_perp R`,
`int ds Box_perp R_nn`

onto the single separated logarithmic coefficient `B_1`. Since the physical log output is one-dimensional, determine whether symmetry/Bianchi identities fix a single linear combination before loop integration. Redundant field-redefinition sectors must remain as consistency checks but not independent physical inputs.

## Claim ceiling

No pole residue or `B_1` value, no noncancellation theorem, no EDT fit or direct EDT/EFT conflict, no continuum EDT, `BRIDGE_DERIVED`, new physics or candidate theory follows.