# ITER116 terminal result — corrected fixed-geodesic curvature `O(G^2)` single-log RG hierarchy

Date: 2026-09-15
Gate: `ITER116_FIXED_GEODESIC_CURVATURE_G2_SINGLE_LOG_RG_HIERARCHY_AUTHORITY`
Preregistration: `ec44eb92a104aaa4a2e41039bf031377404daece`
Source authority: `48cc9022327003e293c8b523386e534b5ec97032`
Adversarial review: `502374547a5567da2274d1ea8d1eb79687c6a4b0`

## Terminal classification

**`PASS_SCOPED_SINGLE_LOG_RG_HIERARCHY_HIGHEST_POLE_DIAGNOSTIC_B1_VALUE_OPEN`**

Bridge credit: **0**.
Candidate theory: **UNFORMED / 0%**.

## Correct RG result

For the corrected separated one-loop basis

`C(l;mu)=G^2 l^-8[B_0(mu)+B_1(mu)L]+O(G^3)`,

`L=log(mu^2 l^2)`,

RG invariance at nonzero separation gives, at the frozen order,

**`mu dB_1/dmu = 0`**

and

**`mu dB_0/dmu = -2 B_1`**.

Thus the logarithmic coefficient is RG invariant at this order, while the finite constant runs to compensate explicit scale dependence.

## Noncancellation shortcut

If `B_1 != 0`, the function `B_0+B_1L` cannot vanish identically for all separations. Therefore a nonzero `B_1` is sufficient to prove a nonzero `O(G^2)` fixed-geodesic curvature tail without computing the complete finite constant `B_0`.

## Highest-pole diagnostic

In the fixed-geodesic renormalization template, the separated logarithm descends from the `H^(2)`-type distribution generated by the strongest UV/double-pole structure. The `H^(1)` sector supplies separated constants and finite-running counterterm contributions.

Hence the net curvature-specific highest-pole / `H^(2)` residue controls `B_1` after complete F/M/G renormalization and tensor reduction.

This reduces the next binary question `B_1 != 0?` to a UV-residue calculation smaller than the full finite one-loop problem.

## Hard ceiling

The nonzero matter-scalar residue cannot be imported to curvature. The curvature lower-order separated correlator is contact-only, and its derivative/composite operator structure is different. The curvature value/sign of `B_1` remains uncomputed.

If the net highest-pole residue vanishes, the finite `B_0` sector still has to be calculated; `B_1=0` would not imply a zero `O(G^2)` correlator.

## Exact successor

`ITER117_FIXED_GEODESIC_CURVATURE_G2_HIGHEST_POLE_RESIDUE_AUTHORITY`

The successor should compute or tightly source-reduce only the coefficients of the highest-pole/H^(2)-type nonlocal structures across F/M/G, preserve gauge/ghost/geodesic counterterms, and test whether the net residue is nonzero. It must stop before finite `B_0` integration unless the residue vanishes.

## Claim ceiling

No value/sign of `B_1`, no noncancellation theorem yet, no EDT fit or direct EDT/EFT conflict, no continuum EDT, no `BRIDGE_DERIVED`, no new physics and no candidate theory follow.